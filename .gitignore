# Maven
target/
*.class

# IDE
.idea/
*.iml
.vscode/
.settings/
.project
.classpath

# Node
dlmm-admin-ui/node_modules/
dlmm-admin-ui/dist/
dlmm-user-ui/node_modules/
dlmm-user-ui/dist/

# Playwright e2e
dlmm-user-ui/test-results/
dlmm-user-ui/playwright-report/
dlmm-user-ui/playwright/.cache/

# Worktree-local config
.claude/launch.json

# OS
.DS_Store
Thumbs.db

# Env
.env
*.log

# WAL-G backup credentials — TD-6, see docs/runbooks/walg-disaster-recovery.md.
# Contains S3 access keys; per-environment, never enters the repo.
docker/.env.walg

# Local DB snapshots — TD-6, see docs/OPS-DB-BACKUP-RUNBOOK.md.
# The pg-snapshot.sh script writes timestamped .dump files here;
# they're per-host and must never enter the repo.
docker/backups/

# Skills (build tool artifacts)
skills/
