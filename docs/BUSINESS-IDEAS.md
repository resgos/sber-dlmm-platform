# Бизнес-идеи — Sber DLMM

Бэклог продуктовых/монетизационных гипотез для платформы концентрированной
ликвидности (рынок РФ, эталон UX — Meteora). Каждая идея: **ценность →
монетизация → усилие (S/M/L)** + что переиспользуем из уже построенного движка.
Не план — гипотезы для приоритизации.

---

## 2026-06-16

### Идея: Проактивный алерт «позиция вышла из диапазона»
Сегодняшний бейдж «Вне диапазона» — пассивный (виден, только если зайти на
страницу). Превращаем его в **событие**: как только активный бин пула выходит
за `[binMin, binMax]` позиции, шлём пуш/колокольчик «Позиция X больше не
зарабатывает комиссии» с кнопкой «Ребалансировать» в один тап.
- **Ценность:** ловит главный момент потери дохода LP именно тогда, когда он
  случился, а не когда пользователь вспомнил зайти; прямой драйвер удержания и
  ребаланс-активности.
- **Монетизация:** часть премиум-уровня алертов (мгновенные vs дайджест);
  апсейл к авто-ребалансу/keeper-подписке.
- **Усилие: S–M.** Per-bin in-range сигнал и `isPositionInRange` уже есть;
  нотификационная инфра (Kafka→notification-service, колокольчик) готова — нужен
  детектор перехода in→out (по событию свопа/смены активного бина) с дедупом,
  чтобы не спамить на каждом тике. Ортогонально «дайджесту уведомлений» (то —
  батчинг общего потока, это — конкретное доходо-критичное событие).

### Идея: Счётчик простоя ликвидности — недополученные комиссии в ₽
Рядом с бейджем OOR показываем **квантифицированную потерю**: сколько времени
позиция вне диапазона и оценку комиссий, упущенных за этот простой
(₽, по среднему fee-rate пула × доле позиции). Бинарный сигнал → конкретная сумма,
которая мотивирует действие сильнее ярлыка.
- **Ценность:** «вы упускаете ≈ N ₽/день, простаивая вне диапазона» — самый
  сильный нудж к ребалансу; одновременно честная аналитика эффективности LP.
- **Монетизация:** премиум-аналитика портфеля; конверсия в авто-ребаланс
  (платим за то, чтобы простой не повторялся).
- **Усилие: M.** Нужно логировать timestamp выхода из диапазона (range-exit) и
  fee-rate окна (24ч-комиссии пула уже считаются); per-bin доля известна. Не
  путать с IL-cover (страхование непостоянных потерь) — здесь именно
  недозаработок от idle, не IL.

### Идея: Предиктивный риск выхода из диапазона (на базе volatility-accumulator)
Предупреждаем **до** простоя: по уже существующему в pool-engine
volatility-аккумулятору (он питает динамическую комиссию) оцениваем вероятность,
что цена выйдет за границы позиции в ближайшие 24ч, и помечаем «край» —
«высокий риск выхода из диапазона при текущей волатильности».
- **Ценность:** опережающий сигнал (центрируй/расширь диапазон заранее) — для
  активных LP это разница между «реагирую на убыток» и «избегаю его»; уникальный
  риск-слой, которого нет у пассивных AMM-витрин.
- **Монетизация:** премиум риск-радар; вход в авто-стратегии «держать в диапазоне».
- **Усилие: M.** Волатильность уже аккумулируется на пул; нужно сопоставить σ с
  расстоянием активного бина до границ позиции (в бинах) и откалибровать порог.
  Отличается от алерта-выхода (реактивный, постфактум) — этот срабатывает заранее.

### Идея: Полная телеметрия свопа в ленте транзакций + детализатор сделки
Аудит ленты вскрыл пробел: у свопов `fee_rate` не пишется вообще (0 из 2088),
`bins_crossed` — лишь у части. Дописываем реальную эффективную ставку (bps) и
число пройденных бинов в транзакцию и раскрываем «детализатор сделки» (клик по
строке → ставка, бины, цена исполнения, проскальзывание факт vs ожидание).
- **Ценность:** прозрачность «во что реально обошёлся обмен» — доверие розницы и
  must-have для профи/комплаенса; основа честной аналитики затрат.
- **Монетизация:** входит в премиум-аналитику/ENTERPRISE-отчётность; снижает
  поддержку («почему столько комиссия?»).
- **Усилие: M.** Кросс-сервис: pool-engine кладёт fee-rate/bins в swap-событие →
  transaction-service персистит (колонки уже есть, пишутся 0). Денежный поток не
  трогаем — только метаданные строки; бэкфилл истории опционален. Сейчас это
  задокументированный латентный пробел (помечен, не чинится вслепую).

### Идея: Агрегатный индекс «Здоровье портфеля»
Сворачиваем per-position health + долю «в диапазоне» (сегодня вывели плиткой
X/N) в один балл 0–100 / гейдж с разбивкой: сколько зарабатывает, сколько вне
диапазона, средневзвешенный health. Один взгляд — и понятно, «как дела у всего
портфеля», а не у отдельных позиций.
- **Ценность:** запоминающаяся «оценка портфеля» — крючок вовлечения и удержания;
  естественный апсейл к авто-ребалансу для тех, у кого индекс низкий.
- **Монетизация:** премиум-аналитика; триггер для платных авто-стратегий.
- **Усилие: S–M.** `calculateHealth` и `summarizePositionRanges` уже есть —
  нужен только композит-вес + гейдж-виджет; чистый фронт, без бэкенда.

### Идея: Экспорт LP-портфеля (CSV/PDF) для отчётности инвестора
По образцу уже готового CSV-экспорта транзакций — выгрузка позиций: пул, диапазон
цен, статус «в диапазоне», текущая стоимость, незабранные комиссии, health,
дата открытия. PDF-вариант — «инвесторская выписка» с шапкой и итогами.
- **Ценность:** закрывает отчётность/бухгалтерию у корпоративных и состоятельных
  LP; брендированный PDF — это ещё и маркетинг при пересылке.
- **Монетизация:** PDF-выписка в премиум-тарифе; брендинг под white-label.
- **Усилие: S.** Переиспользуем `lib/csvExport` и серверные self-describing поля
  позиций; PDF — отдельный лёгкий шаблон. Чистый фронт для CSV.

### Идея: Мультиканальная доставка уведомлений (email / Telegram / web-push)
Сегодня уведомления живут только в приложении (колокольчик + новая страница
истории). Добавляем выбор КАНАЛА доставки по типам: in-app, email, **Telegram-бот**
(особенно актуально для РФ), web-push. Пользователь подписывается на нужные типы
в нужном канале (маржин-колл — в Telegram немедленно, дайджест комиссий — на почту).
- **Ценность:** критичные события (маржин-колл, выход из диапазона) догоняют LP вне
  приложения — прямой драйвер удержания и снижения убытков; Telegram-бот = виральный
  канал входа.
- **Монетизация:** премиум-каналы (Telegram/SMS), приоритетная доставка; B2B-тариф
  с гарантией SLA по алертам.
- **Усилие: M.** notification-service уже публикует события (Kafka) и считает
  типы/непрочитанные — нужен слой доставки (per-channel адаптеры + таблица подписок)
  и UI настроек. Каналы — изоляция от денежного пути.

### Идея: Inline-действия в ленте уведомлений
Сейчас уведомление ведёт на нужную страницу (deep-link). Следующий шаг — действие
ПРЯМО в ленте: на `FEE_ACCRUED` — кнопка «Забрать», на `MARGIN_WARNING` —
«Ребалансировать», на `POOL_PAUSED` — «Вывести ликвидность». Лента превращается из
информера в командную поверхность.
- **Ценность:** убирает трение «увидел → нашёл страницу → сделал»; резкий рост
  доли реакций на критичные события (меньше idle-простоя и ликвидаций).
- **Монетизация:** вовлечённость/удержание; апсейл в авто-действия (auto-claim,
  auto-rebalance) для тех, кто часто жмёт вручную.
- **Усилие: M.** Мутации (claim/remove/rebalance) уже есть на страницах —
  нужно вынести в переиспользуемые действия и связать с типом уведомления (карта
  тип→действие рядом с `notificationDeepLink`). Денежные мутации — те же, что в UI.

### Идея: Webhook/stream уведомлений для институциональных клиентов
Корпоративные/проф-клиенты подписываются на события платформы (маржин-коллы, крупные
свопы по их пулам, пороги комиссий, паузы пулов) через **webhook или websocket** —
чтобы заводить их в собственные системы риск-менеджмента и мониторинга.
- **Ценность:** must-have для институционалов (свой риск-контур), дифференциатор vs
  розничные DEX; повышает «липкость» крупных LP.
- **Монетизация:** входит в ENTERPRISE-тариф; плата за гарантированную доставку и
  историю событий (replay).
- **Усилие: M–L.** Событийная шина (Kafka) уже есть — нужен публичный webhook-реестр
  с подписью/ретраями (или WS-gateway), фильтры по типам/сущностям и rate-limit
  (тарифные уровни уже есть). Только чтение событий — без денежного пути.

### Идея: Мобильные карточки позиций/транзакций вместо широкой таблицы
Сегодняшний фикс (скрытие второстепенных колонок ниже `md`) сузил таблицы
(позиции 1507→1060px), но на 375px они всё ещё скроллятся вбок, а кнопки действий
уезжают вправо. Полноценный Meteora-way: ниже `md` рендерить КАРТОЧКУ на позицию
(пара + health в шапке, диапазон+бейдж, стоимость | незабранные, кнопки «Забрать»/
«Закрыть» прямо в карточке) — без горизонтального скролла.
- **Ценность:** демо с телефона перестаёт выглядеть «недоделанным»; действия
  доступны в один тап без скролла — мобильная конверсия LP-операций.
- **Монетизация:** не прямая — гигиена UX, снижает отвал на мобильных (а это
  значимая доля розницы РФ).
- **Усилие: M.** Все хелперы готовы (`HealthScoreBadge`, `isPositionInRange`,
  форматтеры, обработчики claim/remove) — нужен `Grid.useBreakpoint()` + карточный
  рендер; таблица остаётся на `md+`. Чистый фронт.

### Идея: PWA — установка на главный экран + офлайн-кэш портфеля
Превратить user-ui в установимое PWA (manifest + service worker): «добавить на
главный экран», иконка, сплэш, офлайн-кэш последнего портфеля и котировок (read-only
при потере сети). Нативное ощущение без публикации в сторы.
- **Ценность:** иконка на экране телефона = постоянный канал возврата (retention);
  офлайн-просмотр портфеля — приятная мелочь, которой нет у веб-DEX-конкурентов.
- **Монетизация:** не прямая — удержание и частота захода; основа для будущих
  web-push-уведомлений (см. идею мультиканальной доставки).
- **Усилие: S–M.** Vite-PWA-плагин + manifest + стратегия кэширования (stale-while-
  revalidate для GET-портфеля). Не трогает бэкенд и денежный путь.

### Идея: Нижняя таб-навигация (bottom bar) на мобильном
На телефоне заменить боковое меню/гамбургер на Meteora-style нижнюю панель из 4–5
вкладок (Главная / Пулы / Позиции / Купить-продать / ещё) — навигация большим
пальцем, ключевые разделы всегда под рукой.
- **Ценность:** резко сокращает путь к основным действиям на мобильном; «как в
  приложении» — повышает воспринимаемое качество для инвестор-демо с телефона.
- **Монетизация:** не прямая — UX-гигиена и вовлечённость.
- **Усилие: S–M.** `Grid.useBreakpoint()` уже применяется в `UserLayout`; нужен
  условный bottom-bar (фикс снизу, safe-area) ниже `md`, маршруты те же. Чистый фронт.

### Идея: CI-гард паритета переводов ru↔en (ключи и пропуски)
Сегодня EN-дыры (как хардкод-русский в тикере) ловятся вручную. Добавляем гард в
ряд существующих ратчетов (hex/cyrillic/api-params): падать, если наборы ключей
`ru.json` и `en.json` разошлись (ключ есть в одном, нет в другом) или если в коде
есть `t('...')` на несуществующий ключ.
- **Ценность:** делает «полностью двуязычно» проверяемым инвариантом, а не разовой
  вычиткой; ни одна новая фича не уедет в прод с непереведённой половиной.
- **Монетизация:** не прямая — снижает стоимость поддержки EN и риск «стыдных»
  смешанных экранов на инвестор-демо.
- **Усилие: S.** Рекурсивный diff ключей двух JSON + (опц.) grep `t\('([\w.]+)'\)`
  против объединённого набора; подключить к `lint:all`. Чистый скрипт, как уже
  сделанные гарды.

### Идея: Переключатель валюты отображения (₽ / $ / €)
Язык уже переключается; следующий слой — ВАЛЮТА показа цен. Международный инвестор
видит тикер/портфель/пулы в ₽; дать выбор отображать в $ или € по курсу оракула
(SUSDT/SEUR уже торгуются, FX-цены есть). Конвертация чисто визуальная — сделки
по-прежнему в SRUB.
- **Ценность:** снимает барьер «всё в рублях» для не-РФ инвесторов; усиливает
  EN-витрину как реально интернациональную.
- **Монетизация:** не прямая — расширение аудитории; основа для мультивалютной
  отчётности (premium).
- **Усилие: M.** Курс из `oracle.getPrices` (₽→$/€) + хелпер форматирования,
  подвязанный к стору предпочтений (как язык); НЕ трогать суммы сделок/денежный путь —
  только презентацию.

### Идея: Третья локаль — китайский (中文) для коридора РФ-Китай
Учитывая фокус на РФ-Китай (SCNY/Юань уже в каталоге) и приток китайских
инвесторов, добавить мандаринскую локаль третьим языком — стратегический
дифференциатор, которого нет у локальных конкурентов.
- **Ценность:** прямой доступ к крупной китайской аудитории инвесторов/импортёров;
  синергия с идеей B2B FX-коридора для ВЭД.
- **Монетизация:** не прямая — расширение TAM; маркетинговый козырь для партнёрств.
- **Усилие: M.** i18n-инфра уже мультиязычная (`ru`/`en`) — нужен `zh.json` (перевод)
  + пункт в переключателе; зависит от готовности гарда паритета ключей (см. выше),
  чтобы китайский не разъезжался с базой.

### Идея: Защита от «плохой цены» на свопе (price-guard vs оракул)
Сегодня свежий бейдж «±% к рынку» только ИНФОРМИРУЕТ. Следующий шаг — ЗАЩИТА:
если своп исполняется по цене, сильно отклонённой от оракула (> порога), показать
явное предупреждение и требовать подтверждения (или мягко блокировать) — щит для
розницы от случайного плохого фила / тонкой ликвидности.
- **Ценность:** банковский уровень заботы о клиенте — «Сбер не даст купить дорого»;
  снижает споры и репутационный риск на демо для инвесторов.
- **Монетизация:** не прямая — доверие/удержание; основа для «гарантии лучшей цены»
  как премиум-обещания.
- **Усилие: S–M.** `marketReference` уже считает отклонение; нужен порог-гейт в
  CTA свопа (предупреждение + чекбокс «всё равно обменять»). Чистый фронт; денежный
  путь не трогаем — только добавляем подтверждение перед существующим вызовом.

### Идея: Индикатор расхождения пула с рынком в листинге пулов
Перенести «±% к рынку» из формы свопа в КАРТОЧКУ пула на странице «Пулы»: бейдж,
насколько спот пула отклонён от оракула. Инвестор при выборе пула сразу видит,
где цена «съехала» (риск/возможность), а не узнаёт это только в свопе.
- **Ценность:** для LP — сигнал «здесь спот ушёл от рынка, вход рискован/выгоден»;
  для трейдера — подсветка арбитражных пулов; усиливает страницу выбора.
- **Монетизация:** не прямая — качество discovery-поверхности; крючок к авто-стратегиям
  «держать у рынка».
- **Усилие: S.** Те же `oracle.getPrices` + `marketReference`; в листинге пулов уже
  есть `currentPrice` и символы — добавить бейдж в карточку. Чистый фронт.

### Идея: Арбитраж-радар (расхождение спот↔оракул как алерт/лента)
Системный слой над расхождениями: сканировать все пулы и публиковать те, где |спот −
оракул| > порога, как ленту «арбитражных возможностей» (для трейдеров) и
ребаланс-сигналов (для LP/маркет-мейкеров) — через уведомления/вебхуки.
- **Ценность:** притягивает арбитражёров (они выравнивают цену → польза всем LP) и
  даёт LP повод вернуться; уникально для РФ-витрины.
- **Монетизация:** премиум-лента арбитража / вебхук в ENTERPRISE; commission с объёма,
  который арбитраж приносит.
- **Усилие: M.** `marketReference` + периодический скан пулов (есть шина событий и
  оракул); фильтр по порогу/токену и доставка (см. идею мультиканальных уведомлений).
  Только чтение — без денежного пути.

### Идея: Ввод диапазона ликвидности по ЦЕНЕ, а не по бинам
Сегодняшние пресеты (±N бинов) убрали боль ввода сырых индексов, но Meteora-way —
задавать границы по ЦЕНЕ («от 580 до 590 ₽»). Добавить поля цены min/max, которые
конвертируются в бины по `binStep`, и обратную подпись «бин #X = ~Y ₽».
- **Ценность:** инвестор мыслит ценой, а не бинами; снимает последний барьер входа в
  концентрированную ликвидность для розницы.
- **Монетизация:** не прямая — конверсия в LP-операции; основа для «лимитной» подачи
  ликвидности на конкретный ценовой коридор.
- **Усилие: S–M.** Цена↔бин — чистая `binStep`-математика (есть `activeBinId`,
  `binStep`); поля цены + двусторонняя синхронизация с бин-инпутами. Чистый фронт.

### Идея: Визуальный предпросмотр формы ликвидности (shape preview)
Показать на форме добавления маленький bar-чарт распределения ликвидности по бинам
для выбранной стратегии (SPOT/CURVE/BID-ASK) и диапазона — фирменная «форма» Meteora.
Переиспользовать `BinLiquidityChart` (уже есть на странице пула).
- **Ценность:** «видно, куда лягут деньги» — резко повышает доверие и понимание
  концентрированной ликвидности; сильный визуальный крючок для демо.
- **Монетизация:** не прямая — вовлечённость/конверсия; апсейл продвинутых стратегий.
- **Усилие: S–M.** Компонент `BinLiquidityChart` готов; нужно прокинуть выбранные
  диапазон+стратегию и отрисовать предполагаемое распределение. Чистый фронт.

### Идея: Авто-диапазон по волатильности пула
Кнопка «Оптимальный» рядом с пресетами: рекомендует ширину диапазона по недавней
волатильности пула (volatility-accumulator уже питает динамическую комиссию) — выше
волатильность → шире рекомендованный диапазон (меньше риск выхода).
- **Ценность:** «умный» вход в один клик — снимает главный вопрос новичка «какой
  диапазон выбрать»; данные-driven, а не угадайка.
- **Монетизация:** не прямая — мост к авто-стратегиям/ребалансу (премиум).
- **Усилие: M.** σ из volatility-accumulator (есть на пул) → ширина в бинах по
  откалиброванной формуле; переиспользует сегодняшний `presetBinRange`. Бэкенд-чтение
  волатильности, без денежного пути.

### Идея: Кросс-переход «транзакции по этому пулу» (deep-link фильтра)
Теперь, когда фильтр транзакций по пулу работает, связать поверхности: со страницы
позиции/детали пула — кнопка «История по этому пулу», которая открывает
`/transactions?poolId=…` уже отфильтрованной (фильтры читаются из URL).
- **Ценность:** бесшовный путь «вижу позицию → смотрю её движения» без ручного
  выбора пула; повышает связность и доверие к данным.
- **Монетизация:** не прямая — глубина вовлечения; основа для шеринга
  отфильтрованных выписок.
- **Усилие: S.** Бэкенд `poolId` уже есть; нужно читать/писать фильтры в URL-параметры
  (useSearchParams) и кнопка-ссылка на странице позиции/пула. Чистый фронт.

### Идея: Сводка по выбранному пулу в ленте транзакций
Когда активен фильтр по пулу — показать мини-сводку именно по нему: число операций
по типам (свопы/добавления/заборы), суммарно забранные комиссии, нетто-поток. Лента
превращается в «карточку пула глазами инвестора».
- **Ценность:** инвестор видит итог по конкретному пулу одним взглядом, не считая
  вручную; усиливает разбор «как мне этот пул».
- **Монетизация:** премиум-аналитика по пулу; апсейл к полноценной P&L-аналитике.
- **Усилие: M.** Агрегат можно считать на бэке (та же Specification + group-by) или
  на фронте по выгрузке; аккуратно с кросс-токеном (считать по типам/счётчикам и
  забранным комиссиям в ₽, а не «объём в одной валюте»).

### Идея: Единый фильтр-движок (Specification) на остальные пользовательские ленты
Перенести чистый pool/type/status/date-фильтр (сегодня в транзакциях через
`JpaSpecificationExecutor`) на ленты уведомлений, истории комиссий, авто-claim-лога —
единый предсказуемый набор фильтров во всех списках.
- **Ценность:** консистентный UX («везде фильтрую одинаково») и меньше разнобоя в
  коде; снижает класс багов FE↔BE-рассогласования (есть гард `check-api-params`).
- **Монетизация:** не прямая — качество продукта и скорость новых фич.
- **Усилие: M.** Репозитории перевести на `JpaSpecificationExecutor` + общий
  spec-builder; контроллеры — единый набор `@RequestParam`. Только чтение, без
  денежного пути; покрыть param-гардом и тестами.

## 2026-06-11

### Идея: «Казначейство-as-a-Service» — управляемые LP-волты для корпоратов
Корпоративные клиенты Сбера паркуют простаивающий SRUB/YSRUB в **курируемые
DLMM-стратегии** с авто-ребалансом и SLA по спреду/просадке, не управляя
диапазонами вручную («депозит+», но на маркет-мейкинге).
- **Ценность:** превращает idle-кэш казначейства в доходность от комиссий; клиент
  не вникает в bin-механику — выбирает риск-профиль (консервативный/сбалансир./агро).
- **Монетизация:** management fee (bps на AUM) + performance fee с заработанных
  комиссий; премиум-тариф за гарантированный спред.
- **Усилие: M.** Переиспользуем `AutoClaimScheduler`, `rebalancePlanner`, per-bin
  модель комиссий — нужен волт-обёртка (агрегатная позиция на пул стратегий) +
  отчётность AUM/PnL. Риск-движок IL уже частично есть.

### Идея: B2B FX-коридор для ВЭД (импортёры/экспортёры)
На базе уже существующих пар **SUSDT/SRUB, SEUR/SRUB** — предсказуемая конвертация
валюты для бизнеса с узким спредом (концентрированная ликвидность) и
расчётом «день-в-день» через ACID-ledger.
- **Ценность:** импортёру — прогнозируемый слиппедж и rate-lock на окно оплаты
  контракта; vs банковский кросс-курс — прозрачно и дешевле на объёме.
- **Монетизация:** комиссия за конвертацию + плата за «замок курса» (опцион-подобное
  окно); тариф за приоритетную ликвидность под крупный тикет.
- **Усилие: M.** Свечной движок свопа готов; нужен corporate-онбординг (лимиты,
  мультиподпись), endpoint rate-lock (зарезервировать котировку на N минут) и
  отчёт для бухгалтерии (переиспользуем self-describing `TransactionResponse` —
  символы/пара уже в API).

### Идея: «Ликвидность на Спасибо» — ритейл-лояльность → LP
Дать рознице вносить ликвидность **бонусами Спасибо** (в стеке уже есть
`04-spasibo-seed`) и зарабатывать комиссии в SRUB. Геймификация «Вложи Спасибо»
с маскотом Сберкотом, понятный Simple-режим.
- **Ценность:** монетизирует «спящие» Спасибо, заводит массового пользователя в
  DeFi-механику через знакомый бренд лояльности; виральный retail-funnel.
- **Монетизация:** спред + breakage невыкупленных баллов; апсейл в полноценные
  пары после первого позитивного опыта.
- **Усилие: S–M.** Маскот, Simple-режим и забор комиссий уже есть; нужен мост
  Спасибо↔SRUB (курс/лимиты) + витрина «доходность vs кешбэк».

### Идея: Платный API/SDK для квантов и маркет-мейкеров
Платный API-доступ (повышенные rate-limit, websocket-фиды цен/бинов, программное
управление LP-позициями) для квант-фондов и алготрейдеров — «ликвидность как
инфраструктура».
- **Ценность:** профи приносят объём и узкие спреды (полезно всем LP), а сами
  получают предсказуемый программный доступ; платформа монетизирует то, что и так
  построено.
- **Монетизация:** подписочные тарифы API (платформа уже имеет уровни
  FREE/PRO/ENTERPRISE в JWT-claims и rate-limit на gateway) + плата за websocket-фид.
- **Усилие: S–M.** Тарифы и rate-limit уже есть; нужен публичный API-портал
  (ключи, документация OpenAPI), websocket-стрим бинов/сделок и SDK-обёртка.

### Идея: White-label DLMM-площадка для региональных банков/финтехов
Лицензировать движок + UI как «под ключ» концентрированную ликвидность другим
финансовым институтам РФ — Сбер как инфраструктурный провайдер DeFi-уровня.
- **Ценность:** монетизация уже готовой платформы за пределами Сбера; сетевой
  эффект (общие пулы/ликвидность); стратегическое позиционирование как
  инфра-провайдера.
- **Монетизация:** лицензия + revenue-share с комиссий площадки-партнёра + платная
  поддержка/SLA.
- **Усилие: L.** Нужна мультитенантность (изоляция орг — частично есть `orgId` в
  claims), брендирование темы (CSS-переменные уже выделены), отдельный онбординг и
  биллинг. Стратегический, не быстрый.

### Идея: Страхование непостоянных потерь (IL-cover) для LP
Опциональная платная защита от непостоянных потерь: при заборе позиции, если IL
превысил порог, LP получает частичную компенсацию из страхового пула; премия
считается от волатильности пары.
- **Ценность:** снимает главный страх поставщика ликвидности (IL) — конверсия
  осторожных депозиторов в LP; яркий дифференциатор vs обычный банк-депозит.
- **Монетизация:** страховая премия (% от позиции), маржа страхового пула;
  кросс-продажа к управляемым волтам.
- **Усилие: L.** Нужны актуарная модель премии (есть volatility-accumulator в
  движке), учёт IL на момент входа/выхода (per-bin чекпойнты уже есть) и капитал
  страхового пула. Сначала — пилот на 1–2 парах.

### Идея: DCA-боты и автоинвестирование в ликвидность
Автоматическое усреднение (DCA) + лимитные ордера для розницы: «каждую неделю
добавляй N₽ в пул X», с уведомлениями о статусе. Платформа уже имеет лимитные
ордера (OrdersPage) и нотификации.
- **Ценность:** снимает порог входа («настроил и забыл»), формирует привычку и
  регулярный приток ликвидности; знакомый рознице паттерн автоплатежа.
- **Монетизация:** плата за исполнение ордера или премиум-подписка на автостратегии.
- **Усилие: S–M.** Инфраструктура ордеров и нотификаций есть; нужен планировщик
  повторов + UI мастера автостратегии.

### Идея: Копи-трейдинг ликвидности (соц-слой)
Лидерборд лучших LP по realized-APY с возможностью «повторить стратегию» (какие
пулы/диапазоны выбирает топ-провайдер). Геймификация поверх Сберкота.
- **Ценность:** виральный соц-механизм привлечения; новички копируют проверенные
  стратегии вместо страха перед bin-механикой; доверие через прозрачный рейтинг.
- **Монетизация:** copy-fee / доля от результата ведущему LP + платформе.
- **Усилие: M.** Realized-APY и per-bin данные есть; нужны рейтинг, согласие на
  публичность портфеля и движок «зеркалирования» добавления ликвидности.

### Идея: Авто-налоговый отчёт LP для РФ
Автогенерация годового отчёта о доходах LP (забранные комиссии, P&L) в формате,
удобном для декларации в РФ — реальная боль инвестора в крипто/DeFi.
- **Ценность:** закрывает болезненный комплаенс-сценарий, дифференциатор для
  серьёзных инвесторов; повод вернуться на платформу раз в год минимум.
- **Монетизация:** премиум-фича / плата за отчёт; апсейл к ENTERPRISE-тарифу.
- **Усилие: M.** Данные уже в `transactions` / `fee_accruals` (claimed-история);
  нужен агрегатор по налоговому периоду + экспорт в нужную форму.

### Идея: «Стейкинг витрины» — программируемые маркетинговые пулы
Партнёрские пулы с временным бустом APY, которые бренды/эмитенты токенов
оплачивают как рекламу размещения ликвидности (спонсируемый farming поверх
существующего reward-движка `12-seed-farming-rewards`).
- **Ценность:** эмитенту — гарантированная глубина рынка его актива; LP — буст
  доходности; платформе — новый B2B-канал выручки без изменения core-механики.
- **Монетизация:** размещение (listing fee) + комиссия с бюджета буста.
- **Усилие: S–M.** Farming-конфиги уже есть; нужен админ-флоу «спонсируемый пул»
  + бейдж в каталоге и срок действия буста.

### Идея: Демо-песочница инвестора с виртуальным портфелем
Публичный режим «попробуй без денег»: виртуальный SRUB-баланс, реальный движок
свопов/ликвидности на изолированных пулах, лидерборд доходности за неделю.
- **Ценность:** низкобарьерная воронка (демо уже умеет всё нужное — это
  продуктизация текущего демо-стека); обучение через практику; лиды в KYC.
- **Монетизация:** конверсия в боевые счета; спонсорство конкурсов недели.
- **Усилие: M.** Изоляция org/балансов уже есть (orgId, internal credit);
  нужен авто-онбординг без KYC с лимитами и сброс песочницы по расписанию.

### Идея: RWA-пулы — от SGOLD к облигациям и недвижимости
Расширить линейку реальных активов (SGOLD уже торгуется) токенизированными
корп. облигациями Сбера и паями недвижимости с ликвидностью через DLMM-бины.
- **Ценность:** «биржа реальных активов» с узким спредом — уникальное РФ-предложение;
  консервативному инвестору понятнее крипты; синергия с казначейскими волтами.
- **Монетизация:** комиссия эмиссии/листинга RWA + спред пулов + кастоди.
- **Усилие: L** (юр. обвязка), технически S — каталог токенов и движок уже
  поддерживают любой FIAT_BACKED/RWA-актив.

### Идея: Мерчант-эквайринг поверх пулов (СБП-мост)
Приём оплаты в стейблах (SUSDT и др.) с мгновенной конвертацией в SRUB по
пулу и выплатой мерчанту: «крипто-эквайринг без волатильности».
- **Ценность:** мерчанту — новый платёжный канал с гарантированным курсом
  исполнения (концентрированная ликвидность = предсказуемый слиппедж);
  платформе — стабильный своп-объём (комиссии LP растут).
- **Монетизация:** эквайринговая комиссия + спред; подписка за rate-lock окна.
- **Усилие: M.** Движок свопов готов; нужен merchant-API (инвойс → квота →
  исполнение → вебхук) и связка выплат с СБП.

### Идея: Структурные транши LP (senior/junior)
Разделить пул-позицию на два класса: senior (фикс-доходность, защита от IL за
счёт junior) и junior (плечо на комиссии + остаточный IL-риск).
- **Ценность:** один продукт покрывает два профиля инвестора — «как депозит,
  но выше ставка» и «агрессивный доход»; классика структурных продуктов,
  знакомая банковской рознице.
- **Монетизация:** структуризационная комиссия + performance fee junior-транша.
- **Усилие: L.** Нужен волт-агрегатор с водопадом выплат; per-bin учёт комиссий
  уже даёт точную базу распределения. Пилот на 1 пуле.

### Идея: «Семейный портфель» — мульти-доступ к LP с ролями
Совместное управление позициями для семьи/малых команд: владелец делится
view-only или trade-доступом (orgId/orgRole в JWT уже есть), лимиты на члена.
- **Ценность:** удержание через сетевой эффект домохозяйства; безопасный
  «детский» режим как обучение инвестициям; B2C-аналог корпоративных команд.
- **Монетизация:** семейная подписка (фикс/мес) + апсейл лимитов.
- **Усилие: S–M.** Org-модель и TeamPage уже есть — нужен B2C-онбординг
  «пригласить родственника» + пресеты ролей/лимитов.

### Идея: Голосовой/чат-ассистент инвестора (Сберкот + GigaChat)
Диалоговый слой поверх платформы: «сколько я заработал за неделю?», «переложи
половину SUSDT-позиции в SGOLD», «почему упал APY?» — с подтверждением операций.
- **Ценность:** радикально снижает порог для нетехнической аудитории; Сберкот
  уже маскот-гид — логичная эволюция в полноценного ассистента экосистемы.
- **Монетизация:** премиум-фича PRO-тарифа; рост конверсии в операции.
- **Усилие: M–L.** Все данные доступны по внутренним API; нужен NLU-слой
  (GigaChat), маппинг намерений на API и жёсткие подтверждения для money-операций.

### Идея: Мост в СберИнвестиции — кросс-селл из брокерки
Покупка платформенных активов (SETH, SGOLD, LP-волты) прямо из приложения
СберИнвестиции как «альтернативные инструменты»; единый портфельный вид.
- **Ценность:** мгновенный доступ к многомиллионной брокерской аудитории без
  собственного привлечения; знакомый интерфейс снижает порог.
- **Монетизация:** комиссия дистрибуции с брокерки + рост TVL/объёмов.
- **Усилие: L** (интеграционные согласования), технически M — API уже
  self-describing, нужен B2B-контракт счетов и SSO-мост.

### Идея: Подарочные SRUB-сертификаты (gift-ссылки)
Отправка SRUB/позиции по ссылке/QR: получатель проходит лёгкий онбординг и
получает токены — «подари другу первый вклад в ликвидность».
- **Ценность:** виральная петля привлечения с нулевым CAC; естественный повод
  для первого касания DeFi-продукта.
- **Монетизация:** breakage неактивированных подарков + конверсия получателей
  в активных LP; брендированные корпоративные подарки (B2B2C).
- **Усилие: S–M.** Internal credit/deduct уже есть; нужен escrow-токен ссылки,
  TTL и анти-фрод лимиты.

### Идея: Данные как продукт — исторический маркет-дата фид
Продажа очищенных исторических данных (OHLCV, объёмы, глубина бинов,
agg-метрики ликвидности) исследователям, вузам и квантам — отдельно от
trading-API: бакеты выгрузок + витрина в ClickHouse.
- **Ценность:** RWA/DLMM-данные РФ-рынка уникальны; академия и фонды платят за
  чистые ряды; усиливает бренд «инфраструктурного провайдера».
- **Монетизация:** подписка на фид/выгрузки, тарифы по глубине истории.
- **Усилие: S–M.** ClickHouse уже копит аналитику; нужен экспорт-слой
  (S3/parquet) и биллинг доступа.

### Идея: AML/комплаенс-движок как отдельный продукт
Детекторы паттернов (fast-in-fast-out, round-amount repeats, TVL-аномалии) уже
работают в transaction-service — упаковать их в standalone-API «комплаенс для
финтехов» с вебхуками алертов и отчётами для регулятора.
- **Ценность:** РФ-финтехам нужен готовый транзакционный мониторинг (115-ФЗ);
  у Сбера — доверие бренда + реальные данные для тюнинга правил.
- **Монетизация:** подписка по объёму транзакций + плата за кастомные правила.
- **Усилие: M.** Ядро есть; нужен мультитенантный ingest-API, изоляция данных
  и конструктор правил.

### Идея: Telegram-боты поверх платформенного API
Официальный TG-бот (баланс, котировки, алерты, простые свопы с подтверждением)
+ программа для сторонних ботов через публичный API с ревью.
- **Ценность:** TG — главный канал РФ-крипто-аудитории; ноль-friction доступ
  без установки приложения; алерты возвращают пользователей.
- **Монетизация:** конверсия в полные аккаунты; комиссия с бот-свопов;
  партнёрский revenue-share для сторонних ботов.
- **Усилие: S–M.** API self-describing и нотификации готовы; нужен бот-слой
  (webhook, привязка аккаунта через deep-link) и лимиты на бот-операции.

### Идея: Авто-компаундинг комиссий (реинвест в позицию)
Опция «реинвестировать»: забранные комиссии автоматически добавляются обратно
в ту же LP-позицию (сложный процент) вместо зачисления на баланс.
- **Ценность:** «проценты на проценты» — понятный банковской рознице механизм;
  заметно красивее кривая доходности в портфеле; выше удержание TVL.
- **Монетизация:** небольшая комиссия с авто-реинвеста; апсейл к PRO.
- **Усилие: S–M.** Auto-claim планировщик и addLiquidity уже есть — нужен
  флаг политики «куда направлять» и атомарный claim→add путь.

### Идея: «Миссии ликвидности» — квесты с наградами
Геймифицированные задания (первый своп, неделя в позиции, 3 пула, забор
комиссий) с бейджами Сберкота и буст-наградами фарминга.
- **Ценность:** направляет новичка по золотому пути продукта; +activation,
  +retention; повод для пушей возврата.
- **Монетизация:** косвенная (рост активов/объёмов); спонсируемые миссии
  от партнёров-эмитентов.
- **Усилие: S–M.** События уже в Kafka — нужен счётчик прогресса, каталог
  миссий и выдача наград через farming-движок.

### Идея: Private-banking уровень — персональный LP-консьерж
Для крупных клиентов: персональный менеджер, индивидуальная стратегия
диапазонов, отчётность «как в банке», приоритетная линия и закрытые пулы.
- **Ценность:** переносит знакомую модель private banking в DeFi; снимает
  страх самообслуживания у капитала 10M₽+; высокая LTV.
- **Монетизация:** management fee по AUM-сетке + закрытые продукты.
- **Усилие: M** (процессы/люди больше, чем код): роли и закрытые пулы
  частично есть (orgId, статусы пулов), нужен консьерж-кабинет менеджера.

### Идея: Еженедельный AI-дайджест портфеля от Сберкота
Автоматическое письмо/пуш раз в неделю: доходность позиций, забранные
комиссии, IL, события пулов и 1–2 персональные рекомендации (LLM-генерация
по данным портфеля).
- **Ценность:** возвращает пассивных пользователей; «личный аналитик» без
  усилий; повышает доверие прозрачностью.
- **Монетизация:** удержание/реактивация TVL; расширенный дайджест в PRO.
- **Усилие: S–M.** Все данные в API; нужен планировщик-генератор + шаблоны
  и канал доставки (email/пуш уже есть в notification-service).

### Идея: Встраиваемый своп-виджет (embeddable iframe/SDK)
Виджет «обменять» для партнёрских сайтов и медиа (финпорталы, кошельки):
3 строки кода — и у партнёра свой обменник на ликвидности платформы.
- **Ценность:** дистрибуция без своего трафика; партнёр получает функцию,
  платформа — объём и новых пользователей (deep-link онбординг).
- **Монетизация:** revenue-share комиссии со свопов виджета; брендирование.
- **Усилие: M.** Своп-API готов; нужен изолированный виджет-бандл, partner-key
  c referrer-атрибуцией и лимитами.

### Идея: RFQ-доска для крупных сделок (P2P-OTC с эскроу)
Заявочная доска «куплю/продам объёмом» для тикетов, которым дорог слиппедж
пулов: пользователи публикуют RFQ, контрагенты откликаются, расчёт — через
платформенный эскроу (OTC-тип уже есть в transaction-service).
- **Ценность:** удерживает крупные сделки внутри платформы (сейчас ушли бы на
  внебиржу); пулам — меньше токсичного потока, юзерам — лучшая цена.
- **Монетизация:** комиссия с матчинга + плата за приватные RFQ.
- **Усилие: M.** Леджер/эскроу-механика есть; нужен матчинг-сервис заявок и
  UI-доска с откликами.

### Идея: «Календарь доходности» — выплаты как купоны
Прогнозный календарь: ожидаемые комиссии по позициям и farming-награды,
разложенные по неделям/месяцам, как купонный календарь облигаций.
- **Ценность:** переводит DeFi-доходность на язык, знакомый владельцу ОФЗ;
  планирование кэшфлоу = аргумент для консервативного инвестора.
- **Монетизация:** удержание; экспорт в iCal/брокерские отчёты — PRO-фича.
- **Усилие: S–M.** Realized-история и APY есть; нужен прогнозный разложитель
  и календарный виджет на дашборде.

### Идея: «Чек здоровья пула» — прозрачный аудит-бейдж
Публичный автоматический бейдж на каждом пуле: инварианты (композиция бинов,
liquidity=value, свежесть свечей) проверяются непрерывно, статус виден
инвестору («Проверено: 12.06, 14:00»).
- **Ценность:** доверие через прозрачность — то, чего нет у анонимных DEX;
  превращает внутренние self-check'и сидов в продуктовую фичу.
- **Монетизация:** косвенная (конверсия осторожных), API статусов — в
  data-продукт.
- **Усилие: S.** Инвариант-проверки уже написаны (sql self-checks) — нужен
  переодический job + поле статуса в pools API + бейдж в UI.

### Идея: Симулятор «что если» для позиции
Интерактивный слайдер на странице пула: «если цена уйдёт на ±X% — сколько
комиссий заработаю vs IL потеряю» с графиком (по реальной bin-математике).
- **Ценность:** главный страх LP — непонимание IL; симулятор делает риск
  ощутимым ДО депозита; образовательный эффект + конверсия.
- **Монетизация:** конверсия в депозиты; расширенные сценарии (волатильность,
  объёмы) — PRO.
- **Усилие: M.** BinMath на фронте частично есть (binPriceRange); нужен
  расчётный модуль сценариев + виджет.

### Идея: Авто-вывод комиссий на карту/счёт (fiat off-ramp)
Опция «выводить заработанные комиссии на карту Сбера ежемесячно»: claim →
конвертация в SRUB (quote-only уже есть) → выплата на счёт.
- **Ценность:** замыкает цикл «инвестировал → получаю живые деньги» — главный
  аргумент для консервативного инвестора; ощущение «дивидендов».
- **Монетизация:** комиссия за вывод; план «без комиссии» в PRO.
- **Усилие: M–L** (интеграция выплат), технически quote-only claim и
  планировщик авто-claim уже готовы.

### Идея: Бенчмарк-страница «vs депозит» 
Публичный лендинг-калькулятор: «1 млн ₽ на год: вклад X% vs LP-портфель
(история реальных комиссий платформы)» с честным разбросом и рисками.
- **Ценность:** конверсия холодного трафика через знакомое сравнение;
  прозрачность реальных данных (не обещания) = доверие.
- **Монетизация:** acquisition; SEO-трафик по запросам «куда вложить».
- **Усилие: S.** История комиссий/APY есть; нужна одна страница + методология.

### Идея: Лимитки-как-сбережение: «куплю просадку» автопланы
Шаблоны лимитных ордеров для розницы: «покупать SETH каждый раз при −5% от
текущей цены, бюджетом N₽/мес» — эскроу-механика лимиток уже работает.
- **Ценность:** превращает страх волатильности в стратегию («куплю дешевле»);
  знакомый паттерн отложенного ордера из брокерок, но автоповторяемый.
- **Монетизация:** комиссия исполнения; PRO-шаблоны (ступени, трейлинг).
- **Усилие: S–M.** Эскроу/исполнение лимиток есть (проверено вживую);
  нужен планировщик-генератор ордеров + 3 пресета в UI.

### Идея: Корпоративные «зарплатные» LP-программы
Работодатель (начиная со Сбера) докидывает % к взносу сотрудника в выбранные
консервативные пулы (мэтчинг 1:1 до лимита) — как корпоративная пенсия, но
ликвидная.
- **Ценность:** B2B2C-канал с нулевым CAC; лояльность сотрудников; большие
  стабильные TVL-притоки.
- **Монетизация:** management fee с корпоративного пула мэтчинга.
- **Усилие: M.** Org-роли и internal credit есть; нужен договор-флоу
  работодателя и отчётность по программе.

### Идея: Лента активности пула в реальном времени (social proof)
Живой тикер на странице пула и в каталоге: «5 мин назад: своп 60 тыс ₽»,
«час назад: +ликвидность 120 тыс ₽» — обезличенно, из готовых Kafka-событий.
- **Ценность:** «здесь живые деньги» — главный social-proof для нового
  инвестора; повышает доверие и FOMO-конверсию в депозит.
- **Монетизация:** косвенная (конверсия); виджет-тикер в партнёрские медиа.
- **Усилие: S.** События уже летят (pool-events); нужен буфер последних N
  + websocket/поллинг и строка-тикер в UI.

### Идея: «Сберкот-радар» — еженедельный рейтинг пулов
Авто-подборка «3 пула недели» по живым метрикам (оборот, APY-стабильность,
приток LP) с бейджем в каталоге и пушем подписчикам.
- **Ценность:** снимает паралич выбора новичка; направляет ликвидность в
  здоровые пулы; регулярный повод вернуться.
- **Монетизация:** премиум-размещение для эмитентов (прозрачно помеченное).
- **Усилие: S–M.** Метрики уже считаются; нужен скоринг-джоб + бейдж + пуш.

### Идея: «Пульс портфеля» — нотификации об изменении стоимости
Подписка на дельту портфеля: «за день +1,2% (+74 тыс ₽)» утренним пушем и
порог-алерты («если просадка >3% — сообщить сразу»), на базе уже живых
PoolPriceSync-фидов и нотификаций.
- **Ценность:** ежедневная точка контакта без открытия приложения; контроль
  без тревожности (агрегат, не тики).
- **Монетизация:** retention; тонкие настройки порогов — PRO.
- **Усилие: S.** Цены и нотификации есть; нужен дневной снапшот портфеля
  и шаблон дайджест-пуша.

### Идея: Партнёрская витрина структурных нот на LP-доходности
Упаковка LP-доходности в «ноту» с фиксированными датами и условиями для
дистрибуции через премьер-менеджеров банка (бумага поверх волтов).
- **Ценность:** канал в существующую сеть продаж банка; знакомый формат
  для клиента private/премьер; масштабируемый AUM.
- **Монетизация:** структуризационная комиссия + дистрибуционный спред.
- **Усилие: L** (юр/продукт), технически M — волты+отчётность.

### Идея: PWA-установка с виджетом портфеля
Прогрессивное веб-приложение: установка на домашний экран, оффлайн-обложка,
виджет «портфель сегодня» (iOS/Android) с дельтой за день.
- **Ценность:** иконка на телефоне = ежедневное касание без сторов и
  модерации; виджет возвращает в приложение.
- **Монетизация:** retention/частота сессий; нативный шорткат к свопу.
- **Усилие: S–M.** Vite-PWA-плагин + манифест + service worker; мобильная
  вёрстка уже верифицирована (0 оверфлоу, тёмная тема ок).

### Идея: QR-handoff сессии «с десктопа в руку»
Кнопка «Продолжить на телефоне»: QR с одноразовым коротким токеном —
открывает ту же страницу залогиненным на мобильном.
- **Ценность:** демо-вау и реальный кейс (начал на работе — закончил в метро);
  снижает трение мобильного входа без пароля.
- **Монетизация:** конверсия мобильных сессий; основа для биометрии позже.
- **Усилие: M.** Короткоживущий one-time токен + QR-рендер; refresh-механика
  уже есть, нужен exchange-эндпоинт с жёстким TTL.

### Идея: Админ-консоль как продукт «Оператор площадки»
Упаковать admin-ui (пользователи/KYC, пулы, AML-разбор, OTC desk, когорты)
в роль-базированную консоль для white-label-партнёров (идея white-label уже
в бэклоге — это её операционная половина).
- **Ценность:** партнёру нужен не только движок, но и готовый бэк-офис;
  сокращает time-to-launch площадки с месяцев до недель.
- **Монетизация:** входит в лицензию white-label + плата за доп. роли/SLA.
- **Усилие: M.** Консоль уже работает (проверена вживую); нужны
  мультитенантные фильтры и матрица прав.

### Идея: Алерты аномалий для оператора (admin-ops)
Push/Telegram оператору при: остановке роста свечей, дрейфе инвариантов,
росте 5xx, расхождении TVL-роллапа — то, что loop ловил вручную, как продукт.
- **Ценность:** инциденты замечаются до жалоб клиентов; меньше дежурного
  ручного мониторинга.
- **Монетизация:** в составе Enterprise-SLA; экономия на поддержке.
- **Усилие: S–M.** Метрики уже в Prometheus/Grafana; нужен alert-router в
  notification-service + 5–6 готовых правил.

### Идея: Единый «словарь метрик» платформы (metrics contract)
Зафиксировать словарь бизнес-метрик (объём = SRUB-нога, TVL = сумма резервов,
APY = методика) одним документом + библиотечной функцией на бэке и фронте —
loop трижды чинил один и тот же класс расхождений в разных местах.
- **Ценность:** инвестору и аудитору везде показывают ОДНО и то же число;
  меньше регрессий копи/единиц; быстрее онбординг разработчиков.
- **Монетизация:** косвенная — доверие/аудит; обязательное условие white-label.
- **Усилие: S.** Документ + вынос расчётов в dlmm-common/format-хелперы,
  постепенная миграция экранов.

### Идея: Режим «Демо-сценарист» для пресейла
Кнопка для продавца: разогнать показ — серия заготовленных живых свопов/
добавлений с паузами и подсветкой того, что меняется (объём, свечи, комиссии).
- **Ценность:** идеальное демо без ручного ввода; повторяемость на встречах;
  каждый показ генерирует настоящие данные (наш smoke-конвейер — как фича).
- **Монетизация:** инструмент продаж; ускоряет цикл сделки.
- **Усилие: S–M.** Все вызовы готовы (скрипт smoke = прототип); нужен
  оркестратор сценария + оверлей-подсказки в UI.

### Идея: SLA-витрина аптайма для институционалов
Публичная страница статуса (аптайм сервисов, p95 латентности, история
инцидентов) + приватный SLA-отчёт для корпоративных клиентов — то, что
требует любой казначей перед заводом денег.
- **Ценность:** снимает возражение «а это вообще надёжно?»; обязательный
  атрибут B2B-продаж институционалам; дифференциатор vs анонимные DEX.
- **Монетизация:** входит в Enterprise-SLA; повышает конверсию крупных тикетов.
- **Усилие: S–M.** Health/actuator + Prometheus уже есть; нужен сборщик
  аптайма (внешний пробер) + статус-страница + шаблон отчёта.

### Идея: Песочница на тестовых деньгах (paper-trading)
Режим «демо-счёт»: виртуальный баланс, те же пулы и математика, но без
реальных средств — обучение свопам/LP/комиссиям без риска, с переходом на
боевой счёт в один клик.
- **Ценность:** убирает страх первого касания DeFi у массового РФ-инвестора;
  обучающая воронка; безопасная площадка для контента/вебинаров.
- **Монетизация:** конверсия paper→боевой; обучающий контент как лид-магнит.
- **Усилие: M.** Движок и UI готовы; нужен изолированный «виртуальный» тип
  счёта в token-service (флаг + отдельный кошелёк) и баннер режима.

### Идея: «Спящая ликвидность» — авто-подсказка по пулам с низким APY
Пулы-гиганты с почти нулевым APY (как SBTC 0.01% — много TVL, мало объёма)
детектировать и подсказывать LP: «здесь капитал простаивает — переложите в
пул X с APY N%», прямо на витрине/в дайджесте.
- **Ценность:** повышает реальную доходность LP (меньше мёртвого капитала),
  growth-метрика «активный TVL»; честная забота вместо обмана «0.0%».
- **Монетизация:** удержание + рост оборота (перелив в активные пулы = больше
  комиссий платформе).
- **Усилие: S–M.** APY/объём уже считаются; нужен детектор «idle-пула» +
  ранжирование альтернатив + копирайт подсказки.

### Идея: Бейдж «глубина рынка» на карточке пула
Простой индикатор для новичка: «крупную сделку (1 млн ₽) пул проведёт со
слиппеджем ~X%» — превью из bin-резервов, без раскрытия механики бинов.
- **Ценность:** переводит «концентрированную ликвидность» на язык понятной
  выгоды (мало проскальзывания); помогает выбрать пул осознанно.
- **Монетизация:** конверсия в свопы/депозиты; апсейл аналитики в PRO.
- **Усилие: S.** Симуляция свопа уже есть (quote-эндпоинт); нужен фоновый
  расчёт на фикс-сумму + бейдж.

### Идея: Калькулятор доходности «vs вклад» (развитие)
К уже реализованному калькулятору доходности на странице пула добавить
сравнение с банковским вкладом: тянуть ключевую ставку ЦБ (price-oracle уже
ходит в ЦБ РФ) и показывать «LP ≈ N ₽ vs вклад под X% ≈ M ₽».
- **Ценность:** прямое сравнение с понятным якорем = сильнейший конверсионный
  аргумент для консервативного РФ-инвестора.
- **Монетизация:** конверсия в депозиты; «обогнали вклад» — маркетинг-хук.
- **Усилие: S.** Калькулятор и oracle-фид ЦБ готовы; нужна ставка вклада в API
  oracle + одна строка сравнения в компоненте.

### Идея: Реалистичный прогноз по истории объёма (а не текущему APY)
Вместо линейной экстраполяции текущего APY — прогноз по медиане комиссий за
N последних дней (из fee_accruals/ohlcv), с диапазоном «пессимистично…
оптимистично».
- **Ценность:** честнее и устойчивее к скачкам APY; снимает претензию
  «обещали, не сбылось»; доверие.
- **Монетизация:** удержание; премиум-горизонты прогноза в PRO.
- **Усилие: M.** История объёмов/комиссий есть; нужен агрегат-эндпоинт
  (медиана/перцентили) + замена формулы в калькуляторе.

### Идея: Происхождение оценки APY — бейдж «по факту 24ч» vs «по модели»
Оценка APY теперь либо выводится из реального объёма за 24ч, либо подпирается
модельным полом по тарифу комиссии (когда живого объёма мало). Показывать
пользователю, какая ветка сработала: маленький бейдж/тултип «по факту за 24ч»
против «оценка по модели».
- **Ценность:** честность и доверие; снимает риск претензии «APY завышен»;
  обучает, что APY — это оценка, а не обещание.
- **Монетизация:** доверие → удержание; в PRO — раскрытие методики и история APY.
- **Усилие: S.** Бэкенд уже различает обе ветки в `estimateApyPercent`; добавить
  поле `apySource: LIVE|MODEL` в `PoolResponse` + бейдж на карточке/детали пула.

### Идея: «Эффективная ставка» позиции — реальный реализованный APY
На странице позиций показывать по каждой позиции фактический APY =
(забранные + накопленные комиссии) / депозит / возраст, в годовых — «ваша
позиция реально приносит X%», рядом с оценочным APY пула.
- **Ценность:** переводит абстрактный APY пула в «мои деньги»; самый честный
  показатель доходности; сильный аргумент удержания и доверия.
- **Монетизация:** удержание; в PRO — график реализованного APY во времени и
  выгрузка для налогового отчёта.
- **Усилие: M.** Данные есть (единый источник комиссий per-bin owed,
  `initial_deposit`, `created_at`); нужен расчёт realized-APY в getUserPositions
  + колонка/карточка в UI.

### Идея: Фильтр пулов «только с реальной доходностью» (LIVE-only)
Раз у APY появилось происхождение (LIVE vs MODEL), дать в списке/сравнении
пулов чип-фильтр «только с реальным объёмом» — скрыть модельные оценки и
оставить пулы, чей APY подтверждён фактической торговлей за 24ч.
- **Ценность:** PRO- и институциональные инвесторы хотят реальные, а не
  модельные числа; прозрачность площадки.
- **Монетизация:** PRO-фильтр; аргумент для институционального сегмента.
- **Усилие: S.** Тот же флаг `apySource` (идея выше) + фильтр-чип и предикат
  сортировки на `PoolsPage`/`PoolComparePage`.

### Идея: Сохранённые фильтры-отчёты («Мои выгрузки»)
На странице транзакций дать сохранять комбинацию фильтров (тип + статус +
диапазон дат) под именем и переоткрывать одним кликом — для регулярной
отчётности (например, «свопы за месяц»). Теперь, когда фильтры реально
работают на бэке (type/from/to через Specification), это надстройка над ними.
- **Ценность:** экономит рутину бухгалтеру/инвестору; «отчётность в один клик».
- **Монетизация:** удержание; в PRO — общий доступ к сохранённым отчётам в команде.
- **Усилие: S.** Чисто фронт: localStorage-стор (как `positionAlertsStore`) +
  выпадающий список «Мои выгрузки» рядом с фильтрами; экспорт уже есть.

### Идея: Серверный потоковый экспорт больших историй
CSV-экспорт сейчас клиентский, с потолком 5000 строк. Для институционалов с
длинной историей — серверный стриминговый эндпоинт `/transactions/me/export`
(CSV/XLSX) поверх той же Specification-фильтрации, отдающий весь срез потоком.
- **Ценность:** снимает потолок; XLSX-формат привычнее для финслужб.
- **Монетизация:** ENTERPRISE-фича; основа для подписи/штампа отчёта.
- **Усилие: M.** Эндпоинт со `StreamingResponseBody` + переиспользовать
  Specification из `getUserTransactions`; на фронте — переключатель «весь срез».

### Идея: Годовой налоговый отчёт (3-НДФЛ-ready)
Свести по году реализованный P&L и уплаченные/полученные комиссии в разрезе
токенов в форму, пригодную для декларации РФ-инвестора. Источник — те же
транзакции + история комиссий (`fee_accruals`).
- **Ценность:** снимает главную боль РФ-инвестора (налоговая отчётность);
  сильный аргумент доверия и удержания на годовом горизонте.
- **Монетизация:** PRO/ENTERPRISE; партнёрство с налоговыми сервисами.
- **Усилие: L.** Нужен агрегат realized-P&L по FIFO/средней + сводка комиссий +
  форматтер отчёта; считать на бэке, выгрузка PDF/XLSX.

### Идея: Авто-сплит крупной сделки (TWAP-lite)
Когда влияние на цену заметно (квота вернула высокий priceImpact), предложить
разбить сделку на N частей и исполнить последовательно — суммарно меньше
проскальзывания, чем один большой своп. Кнопка «Исполнить в N частей».
- **Ценность:** прямая экономия для крупного ордера; «умное исполнение» как у
  топовых DEX/брокеров — аргумент для активного трейдера.
- **Монетизация:** PRO-фича; основа для лимитной/алго-витрины.
- **Усилие: M.** priceImpact уже в квоте; нужен сплиттер (N пере-квот +
  последовательный executeSwap с общим idempotency-префиксом) и индикатор прогресса.

### Идея: Мягкая подсказка Сберкота о крупной сделке (Simple)
В Simple демо-пулы глубокие — влияние редко превышает доли процента, так что
жёсткий порог-предупреждение (5%) не срабатывает. Вместо него — дружелюбная
подсказка Сберкота, когда impact выше «типичного для пула» (например, p90 по
истории или просто >0.3%): «Крупная сделка чуть двигает цену — это нормально».
- **Ценность:** обучает новичка без алармизма; повышает доверие к цифрам.
- **Монетизация:** вовлечённость/удержание; обучающий онбординг.
- **Усилие: S.** Порог от типичного impact пула + один тост/баббл Сберкота на
  странице Simple-свопа.

### Идея: Подсказка «глубины» на форме свопа — до какой суммы цена почти не двигается
Ещё до ввода суммы показывать на свопе ориентир «до ≈ N ₽ влияние < 0.5%» —
оценка по ликвидности активного диапазона бинов. Помогает выбрать размер сделки.
- **Ценность:** проактивно (в отличие от бейджа глубины на карточке пула —
  здесь это рекомендация размера прямо в форме); снижает «сюрприз» по impact.
- **Монетизация:** удержание; PRO — точная кривая impact(сумма).
- **Усилие: M.** Нужен endpoint/расчёт «макс. сумма при заданном impact» из
  reserves активных бинов (бинарный поиск по той же swap-математике).

### Идея: Комплаенс-экспорт админ-фида транзакций
Раз у админ-фида транзакций заработали фильтры тип+статус+даты — дать админу
выгрузку отфильтрованного среза (CSV/XLSX) с колонками для комплаенса: юзер,
AML-флаг, reviewedBy/reviewedAt, сумма в SRUB. Отдельно от пользовательского
экспорта (там нет чужих юзеров/ревью).
- **Ценность:** регуляторная отчётность и расследования в один клик — must-have
  для финплощадки под надзором ЦБ.
- **Монетизация:** часть ENTERPRISE/комплаенс-тарифа площадки-оператора.
- **Усилие: M.** Переиспользовать ту же Specification-фильтрацию + стримовый
  эндпоинт в admin-bff; колонки из уже отдаваемых полей транзакции.

### Идея: Быстрые «срезы» аудита для админа (пресеты фильтров)
Кнопки-пресеты над таблицей админ-транзакций: «за сегодня», «крупные > N»,
«только неуспешные», «по подозрительным» — мгновенно выставляют комбинацию
тип+статус+даты (теперь это реально работает на бэке).
- **Ценность:** ускоряет рутину дежурного комплаенс-офицера; меньше ошибок
  ручной фильтрации.
- **Монетизация:** удержание оператора; основа для алертов-подписок на срез.
- **Усилие: S.** Чисто фронт admin-ui: набор пресетов → setFilters; бэкенд уже готов.

### Идея: Журнал действий администратора (audit trail)
Фиксировать действия админа (пометка «проверено», просмотр срезов чувствительных
данных, изменения параметров пула/комиссий) в неизменяемый журнал — для
регуляторной прослеживаемости «кто что и когда».
- **Ценность:** требование надзора (кто менял параметры/смотрел данные); снимает
  риск споров и инсайда.
- **Монетизация:** обязательный модуль для банковского/лицензируемого развёртывания.
- **Усилие: L.** Нужна таблица audit_log + перехват админ-действий (аспект/фильтр
  в admin-bff) + просмотр с фильтрами; интеграция с SIEM опционально.

### Идея: Категорийный бейдж колокольчика уведомлений
Прямой потребитель нового эндпоинта unread-count-by-type: в шапке колокольчика
показать разбивку непрочитанного по категориям с иконками («⚠ 3 маржин-колла,
₽ 2 начисления»), а не один общий счётчик. Клик по категории — фильтр списка.
- **Ценность:** пользователь сразу видит, ЧТО требует внимания (маржин-колл ≠
  начисление комиссии); меньше тревоги от «99+».
- **Монетизация:** вовлечённость/удержание; приоритет критичных алертов.
- **Усилие: S.** Эндпоинт готов; нужен фронт-бейдж в `NotificationBell` (иконки
  на тип уже есть в схеме типов).

### Идея: Тихие категории и дайджест уведомлений
У демо-юзера 167 непрочитанных MARGIN_WARNING — классическая усталость от
уведомлений. Дать настройку «приглушить категорию» / «слать дайджестом раз в
день» по типу (маржин-предупреждения — в сводку, маржин-коллы — сразу).
- **Ценность:** снимает спам, сохраняет критичные алерты — прямой UX-выигрыш и
  доверие; меньше отписок.
- **Монетизация:** удержание; тонкие настройки — в PRO.
- **Усилие: M.** Таблица user_notification_prefs + фильтр на стороне
  notification-service consumer (приглушённые не создаются/группируются).

### Идея: Реконсиляция legacy-типов уведомлений (тех-долг)
В БЕ-фиде уведомлений лежат значения type вне текущего enum (SWAP_CONFIRMED,
LIQUIDITY_ADDED, SUSPICIOUS_ACTIVITY) — из-за этого листинг `/notifications/me`
может падать 500 на странице с таким рядом (Hibernate не материализует enum).
Нужна сид-миграция (переименовать в актуальные значения) + толерантное чтение.
- **Ценность:** убирает скрытый 500 в ядре UX (колокольчик); чистит демо-данные.
- **Монетизация:** надёжность площадки (косвенно).
- **Усилие: M.** SQL-миграция маппинга старых→новые типы + `@PostLoad`/конвертер
  с фолбэком на UNKNOWN; тест на ряд с неизвестным типом.

### Идея: Мобильный bottom-sheet для свопа/ввода ликвидности
На мобиле торговая форма прокручивается простыней; вместо этого — нативный
bottom-sheet (Meteora-mobile-стиль): свайп снизу, крупные тапы, MAX/проценты,
итог сделки закреплён внизу над кнопкой.
- **Ценность:** мобайл — основной канал РФ-ритейла; «как в банковском приложении»
  снижает порог входа и отказы на узком экране.
- **Монетизация:** конверсия мобильных сессий; PWA-установка как следствие.
- **Усилие: M.** AntD Drawer (placement="bottom") + адаптив уже частично есть
  (#32 mobile-fixes); вынести SimpleTrade-форму в шторку под брейкпоинт.

### Идея: Сбер Спасибо — бонусы за объём и LP-активность
Уже есть `SpasiboWidget` — развить в программу лояльности: начислять «спасибо»
за объём свопов и за удержание ликвидности (баллы → скидка на комиссию или
вывод). Нативный для Сбера ритейл-крючок.
- **Ценность:** мощный мост к экосистеме Сбера и эмоциональная привязка
  розничного клиента; узнаваемый бренд лояльности.
- **Монетизация:** удержание/частота; ко-маркетинг со Спасибо; платный буст баллов.
- **Усилие: L.** Нужен начислитель баллов (consumer объёма/комиссий) + витрина
  баланса/обмена + правила; интеграцию со Спасибо начать со «внутренних» баллов.

### Идея: Реферальная программа LP («приведи друга — дели комиссию»)
Персональная ссылка-приглашение; реферер получает долю комиссий/баллов от
свопов приглашённых N дней. Виральный рост базы ликвидности.
- **Ценность:** дешёвый канал привлечения LP/трейдеров; РФ-аудитория любит
  реферальные механики (банки/маркетплейсы приучили).
- **Монетизация:** рост TVL/объёма → рост протокольных комиссий; tiered-бонусы.
- **Усилие: L.** Таблица referrals + атрибуция при регистрации + начисление доли
  (переиспользовать fee/Spasibo-начислитель) + страница «Мои приглашения».

### Идея: Ратчет «без хардкод-кириллицы в .tsx» (i18n-линт)
По образцу hex-check: скрипт, падающий на строковых литералах с кириллицей в
.tsx вне `t(...)` (с baseline-исключениями для комментариев). Колокольчик был
полностью на русском мимо i18n — наверняка есть и другие. Ратчет не даёт
накапливать непереводимые строки.
- **Ценность:** гарантирует, что EN-витрина реально английская; защита от
  регрессий локализации в CI.
- **Монетизация:** косвенно — выход на иностранных/институциональных инвесторов.
- **Усилие: S.** Скрипт по аналогии с `check-no-hex-in-tsx.mjs` + baseline.json;
  подключить в `lint:all`.

### Идея: Глобальное форматирование чисел/дат по языку
Сейчас формат относительного времени правится точечно (инстанс dayjs в
колокольчике). Сделать единый провайдер локали: dayjs + Intl.NumberFormat
(разделители, валюта ₽/₽) переключаются вместе с i18n.language централизованно.
- **Ценность:** консистентный вид для EN-аудитории (1,234.56 vs 1 234,56;
  «5 minutes ago»); меньше точечных правок.
- **Монетизация:** профессиональный вид для инофонда.
- **Усилие: M.** Хук/контекст на смену языка → dayjs.locale + фабрика
  форматтеров; заменить разрозненные форматтеры на общий.

### Идея: Английская инвест-витрина (EN landing + демо-тур)
Точка входа на английском для иностранного/институционального инвестора:
лендинг с тезисами (DLMM, комиссии, TVL), переключатель языка на видном месте,
гайд-тур по ключевым экранам на EN.
- **Ценность:** расширяет аудиторию демо за пределы РФ-ритейла; помогает
  пресейлу с инофондами/партнёрами.
- **Монетизация:** новый сегмент инвесторов; партнёрские интеграции.
- **Усилие: M.** Лендинг-страница + довести EN-перевод ключевых экранов
  (ратчет выше гарантирует полноту) + EN-вариант гайд-тура.

### Идея: Онбординг-чеклист первых действий
Для нового инвестора — закреплённый чеклист с прогрессом: «1) купи актив, 2)
добавь ликвидность, 3) забери комиссию, 4) включи авто-claim». Каждый шаг —
дип-линк на нужный экран, галочка по факту совершённого действия.
- **Ценность:** ведёт новичка по «воронке ценности» (своп → LP → доход);
  снижает отвал на пустом дашборде, ускоряет «ага-момент».
- **Монетизация:** активация/удержание; конверсия в LP (рост TVL).
- **Усилие: S.** Прогресс выводим из уже имеющихся данных (есть позиции? были
  свопы? есть claim?); компонент-карточка на дашборде + дип-линки.

### Идея: Карточка «поделиться доходностью» (share card)
Сгенерировать красивую картинку с APY/доходностью позиции (брендинг Сбер/Меteora-
стиль) для шеринга в мессенджеры/соцсети — без приватных сумм, только проценты.
- **Ценность:** виральный канал привлечения; социальное доказательство
  доходности LP; РФ-аудитория активно делится «скринами прибыли».
- **Монетизация:** дешёвый рост базы; реф-ссылка на карточке (см. реферальную идею).
- **Усилие: M.** Рендер картинки (canvas/og-image) из данных позиции +
  Web Share API; тумблер «скрыть суммы».

### Идея: LP-стрик и достижения (геймификация удержания)
Стрик «N дней с активной ликвидностью», бейджи-достижения (первый своп,
1000 ₽ комиссий, удержание 30 дней). Лёгкая геймификация под РФ-ритейл.
- **Ценность:** удержание и регулярность; эмоциональная привязка; синергия со
  Спасибо-баллами (см. идею Спасибо).
- **Монетизация:** частота/удержание; редкие бейджи → буст баллов.
- **Усилие: M.** Считалка стриков/достижений (по транзакциям/позициям) + витрина
  бейджей; пуш-триггеры через notification-service.

### Идея: Переключатель таймфреймов на графике пула
Прямой потребитель нового OHLCV-роллапа: кнопки 1м/5м/15м/1ч (и далее 4ч/1д,
когда накопится история) над графиком цены пула. Бэкенд уже отдаёт любой
кратный 60с интервал.
- **Ценность:** базовое ожидание трейдера; «как в TradingView/Meteora» —
  серьёзность площадки; разные горизонты для скальпера и LP.
- **Монетизация:** удержание активных трейдеров; PRO — расширенные таймфреймы.
- **Усилие: S.** Segmented над `PoolPriceChart`, прокинуть `interval` в
  `oracle.getOhlcv`; роллап готов.

### Идея: Индикаторы на графике (MA + бары объёма)
Поверх свечей — скользящие средние (MA/EMA по close из тех же свечей) и
гистограмма объёма (volume_in уже в свече). Базовый теханализ без внешних либ.
- **Ценность:** привычный инструментарий трейдера прямо в платформе; дольше
  держит на экране пула.
- **Монетизация:** PRO-набор индикаторов; пресеты.
- **Усилие: M.** Чистые расчёты MA/EMA на фронте по свечам + серии в
  lightweight-charts (объём — гистограмма-overlay).

### Идея: Оверлей внешнего ориентира цены на графике
Наложить на свечной график пула линию внешнего ориентира (рыночная/ЦБ цена из
price-oracle ExternalPriceRef) — видно расхождение цены пула и «справедливой».
- **Ценность:** честность и доверие (видно арбитражный спред); сигнал к свопу
  при отклонении; обучающий момент про DLMM-цену.
- **Монетизация:** доверие→удержание; алерты на расхождение в PRO.
- **Усилие: M.** Эндпоинт истории внешней цены (или текущая линия) +
  доп. серия-overlay на графике; ось уже в Y/X.

### Идея: Гард на рассогласование query-параметров FE↔BE (контракт-тест)
За последние тики трижды всплыл один класс бага: фронт шлёт параметр с одним
именем (txType / dateFrom / email), а бэк читает другое (type / from / query) —
фильтр молча не работает. Нужен авто-тест/линт: собрать имена query-параметров
из FE api-клиентов и сверить с `@RequestParam`/OpenAPI бэка; падать на
несовпадении.
- **Ценность:** убивает целый класс «тихих» багов фильтрации до прода; экономит
  часы отладки «почему фильтр не фильтрует».
- **Монетизация:** косвенно — надёжность площадки и доверие.
- **Усилие: M.** Парс FE `params:{…}` + бэковый OpenAPI (`/v3/api-docs`) или
  grep `@RequestParam`; матчинг по эндпоинту; baseline под легаси, как у ratchet.

### Идея: Серверная фильтрация админ-списка юзеров (KYC/роль/поиск)
Сейчас поиск по имени/почте уже серверный (после фикса), но KYC/роль-фильтры —
клиентские (только текущая страница). Прокинуть их в существующие серверные
параметры (`kycStatus`, `role`) — фильтр работает по всей базе, не по 20 строкам.
- **Ценность:** на проде сотни юзеров — клиентский фильтр находит не всех;
  оператору нужна корректная выборка.
- **Монетизация:** надёжность админ-продукта «Оператор площадки».
- **Усилие: S.** Прокинуть kycStatus/role в `getUsers`-параметры (бэк уже
  поддерживает: query > kycStatus > role) + убрать клиентский `.filter`.

### Идея: Расширенные фильтры оператора по юзерам (дата/баланс/активность)
Дать админу фильтры «зарегистрированы за период», «с балансом > N», «активны за
N дней», сортировки — для сегментации и поддержки.
- **Ценность:** быстрый таргетинг когорт (новички, киты, спящие) для саппорта и
  ретеншн-кампаний.
- **Монетизация:** часть ENTERPRISE-тарифа оператора; основа сегментных рассылок.
- **Усилие: M.** Расширить `searchUsers` (доп. предикаты через Specification) +
  фильтры в admin-ui.

### Идея: Типобезопасный FE-клиент из OpenAPI (codegen)
Радикальное решение класса «рассогласование FE↔BE»: генерировать api-клиент и
типы фронта из бэковых OpenAPI-спек (springdoc уже отдаёт `/v3/api-docs`).
Параметры/пути/типы ответов становятся источником истины — рассинхрон
невозможен by construction.
- **Ценность:** убивает целый класс багов (параметры, формы ответа, типы) и
  ускоряет разработку; меньше ручной синхронизации.
- **Монетизация:** косвенно — скорость и надёжность поставки.
- **Усилие: L.** Подключить openapi-typescript/orval, собрать спеки 8 сервисов,
  поэтапно мигрировать api-слой; переходный период с ручными клиентами.

### Идея: Контракт-тест формы ответа (FE-типы ↔ BE-DTO)
Гард на параметры (готов) ловит запрос; нужен парный — на ОТВЕТ: сверять поля
FE-интерфейсов (`api/types.ts`) с полями BE-DTO/OpenAPI-схем. Ловит «фронт ждёт
поле X, бэк его переименовал/не отдаёт» → undefined в UI.
- **Ценность:** ловит вторую половину контракта (это уже кусало: B3-аудит,
  estimated* → friendly-имена); меньше «пустых» полей в UI.
- **Монетизация:** надёжность; меньше регрессий при изменении DTO.
- **Усилие: M.** Парс FE `interface`-полей + BE record/`@Schema` (или OpenAPI),
  матч по имени DTO/эндпоинту; baseline под легаси.

### Идея: Расширить param-гард на тело запроса и покрытие
Сейчас гард проверяет query-параметры. Добавить: (1) поля тела POST/PUT
(`apiClient.post(path, body)` vs `@RequestBody` DTO), (2) отчёт о «непокрытых»
FE-вызовах (путь не сматчился с бэком — возможный мёртвый/неверный эндпоинт).
- **Ценность:** расширяет защиту на тела (там тот же класс багов) и подсвечивает
  «висячие» вызовы.
- **Монетизация:** косвенно — надёжность.
- **Усилие: M.** Достроить парсер тела/DTO + режим `--coverage`; аккуратно с
  ложными срабатываниями (как уже сделано для query).

---

> Идея становится задачей только после явной приоритизации. Добавляя новые —
> читать файл целиком и не дублировать уже записанное.
