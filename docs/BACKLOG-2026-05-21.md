# Full Backlog — 2026-05-21

Snapshot after this session's Meteora-research + bin-distribution fixes.
Items ordered by priority within each section. Effort guide:
**S** ≤ ½ day, **M** ½–2 days, **L** > 2 days.

> **Sprint 9-DS-r4 update (2026-05-21):** P0-2, P0-3, P0-4, P0-5 shipped.
> P0-1 has foundations laid (`LbDlmmMath` helper + canonical-formula unit
> tests in `dlmm-common`); full migration of `PoolBin.liquidity` readers
> deferred to Sprint 10 because of swap-pipeline regression risk.

---

## P0 — Critical (correctness blockers)

| # | Item | Where | Effort | Status |
|---|---|---|---|---|
| P0-1 | **Canonical LB-DLMM L-units refactor.** Today `liquidity` per bin is stored as mixed-unit sum `amountX + amountY`. compositionFactor = reserveY / liquidity drifts past 1.0 after multiple adds; we clamp [0,1] tactically. Proper fix: switch internal accounting to canonical "L" = `reserveX · binPrice + reserveY` (Trader Joe LB §3.1, not the √(x·y) Uniswap form), with allocation distributing in L-units. Unlocks strategy weights that don't bias by mixed-unit price. | `LiquidityService`, `BinMath`, `PoolBin.liquidity`, all readers | L | **partial** — `LbDlmmMath` helper + canonical-formula tests landed; reader migration deferred Sprint 10 |
| P0-2 | **Bin-anchor convention.** Seed pools use `activeBinId = 2^23` as the anchor where `price ≡ basePrice`. `BinMath.binPrice(basePrice, binStep, binId)` still treats binId as offset from binId 0. Two callers were fixed by subtracting `activeBinId`; remaining callers + tests need audit. Better: add `BinMath.binPriceAtBin(pool, binId)` helper and migrate every site. | `BinMath`, `LiquidityService`, `SwapService`, `MarginWatchService`, pool-engine tests | M | **done** — `BinMath.binPriceAtBin` added; all 3 business call sites migrated; covered by new `BinPriceAtBinTests` |
| P0-3 | **JPA `ddl-auto: validate` drift watchdog.** notification-service crashed on boot today because seed DB had column `read` but entity wanted `is_read`. Same class of bug could land on any service after a rebuild. CI lint to compare every `@Entity` field against the Liquibase changesets that should produce it. | new `dlmm-common-test-fixtures` module + GH Actions step | M | **done** — `.github/workflows/schema-drift.yml` runs `docker compose up --wait` (Postgres init-db.sql + every service's Liquibase + JPA validate must reconcile within 10 min or job fails) |
| P0-4 | **SwapEventConsumer dedup by txId.** Today's consumer dedupes by `idempotencyKey` only; events lacking the key (every swap before Sprint 9-DS-r2) bypass and could duplicate on consumer restart. Add `findBySourceTxId(uuid)` lookup. | `dlmm-transaction-service` | S | **done** — new `pool_engine_tx_id` UUID column (UNIQUE) on `transactions` table; consumer dedups on both keys and treats DB unique-violation as duplicate |
| P0-5 | **Outbox cleanup.** OutboxDispatcher drained 1830 backlogged events on first run today — never garbage-collected. Add scheduled DELETE for `processed_at < now() - 7 days`. | `dlmm-common.outbox` | S | **done** — `OutboxDispatcher.cleanup()` cron job (`dlmm.outbox.cleanup-cron`, default 03:17 daily) deletes published rows older than `dlmm.outbox.retention-days` (default 7); set retention=0 to disable |

---

## P1 — High value (next sprint)

> **Sprint 9-DS-r4 P1 sweep (2026-05-21):** Wave 1 shipped P1-2, P1-5,
> P1-6, P1-7, P1-9, P1-13, P1-15. Wave 2 shipped P1-1, P1-8, P1-10,
> P1-12, P1-14. Wave 3 shipped P1-4 + P1-11. Wave 4 shipped P1-17
> (Spasibo write-back MVP). **15 of 18 P1 items now done**; remaining
> 3 (P1-3 Limit Orders + P1-16/18 monetisation L+) carried to Sprint 10.
>
> P2 waves 1+2+3 shipped P2-1/2/3/4/5/6/7 (verified)/9/10/11/12/13.
> **Wave 4 (this commit batch)** ships **P2-8, P2-14, P2-15, P2-16, P2-17**
> — **17 of 17 P2 items now done**. Only P2-8 (KYC upload) ships as a
> Sber-ID-integration stub (UX shell + client-side validation; real
> upload swaps in when the API contract lands Sprint 10).
>
> TD wave 1 shipped TD-3/7/9/10 (TD-8 already done via P2-11). **Wave 2
> (this commit batch)** ships **TD-2 (secrets runbook + production
> checklist)** and **TD-6 (DB backup script + ops runbook)** — **7 of
> 10 TD items now done**. Remaining 3 (TD-1 Liquibase hack, TD-4 CI/CD
> pipeline, TD-5 Helm chart) all carry L-effort tags and remain
> Sprint-10 work.

### Pool-page Meteora parity

| # | Item | Effort | Status |
|---|---|---|---|
| P1-1 | **Visual price-range slider** with draggable Min/Max handles overlaid on the bin chart. Meteora's killer Create-Position widget; we have numeric inputs + ±5/±10/±20 quick-picks only. | M | **done** — new `<BinRangeSlider>` (AntD `<Slider range/>`) on `PoolAddLiquidityPanel`. Domain ±50 around active; handle tooltip shows the actual price (Y per 1 X) via the DLMM log-spaced formula; numeric inputs stay for power users who need a wider range |
| P1-2 | **Strategy preview redraws the bin chart.** Clicking Spot/Curve/Bid-Ask in the right rail should redraw the user-bin overlay in the chart to show what *would* be allocated before submit. Backend has the math (`calculateDistributionWeights`); FE just needs to mirror it. | M | **done** — `lib/strategyWeights.ts` mirrors the backend formulae; `PoolDetailPage` lifts pending `{binMin,binMax,strategy}` from `PoolAddLiquidityPanel` into `BinLiquidityChart` which renders the weighted overlay as a second orange Bar series; cleared on tab switch / unmount |
| P1-3 | **Limit Orders.** Third tab on Meteora's right rail. Single-bin position that auto-closes when crossed. Could be a thin wrapper around `addLiquidity` with `binMin=binMax` + a watcher that auto-removes when active bin crosses. New monetisation: takes a small "limit order bonus" fee. | L | open — Sprint 10 |
| P1-4 | **TradingView candlestick chart** in the centre column. Meteora uses TradingView Charting Library; we can use `lightweight-charts` (free) hooked to `dlmm-price-oracle` OHLCV. Already have CBR fixings; add 1m/5m/1h aggregation. | M | **done** — `lightweight-charts@4.2` on `PoolDetailPage` above the bin distribution. Single candlestick series + volume histogram overlay (bottom 20%). Reads from new `/api/v1/oracle/ohlcv/{poolId}` (P1-11). Refetches every 30s — matches the aggregator flush cadence so new candles arrive within ~half a flush. |
| P1-5 | **Zoom in/out on Liquidity Distribution chart.** Meteora has +/− buttons that re-window the bins around active price. We show fixed ±25. | S | **done** — `BinLiquidityChart` now has +/− and aim (recenter) controls; zoom levels ±5/±10/±25/±50/±100; window auto-extends to cover user-pinned + preview ranges |
| P1-6 | **Pool-scoped recent transactions feed.** Centre-bottom panel on Meteora ("Positions / Limit Orders / **History**"). Last 20 swaps with size/direction/price impact for *this* pool. We have `/transactions/me`; add `/pools/{id}/transactions?limit=20`. | S | **done** — new `GET /api/v1/transactions/pool/{poolId}` (CONFIRMED swaps only, server-clamped to [1,100]); new `<PoolRecentSwapsPanel>` slot below the bin chart on `PoolDetailPage` |
| P1-7 | **"Cost required to create 1 position"** estimate on the Add Liquidity panel. Meteora shows ~0.05746 SOL gas estimate. For us: roughly `numBins × bin_creation_fee + protocol_fee_at_active`. Static for now, exact later. | S | **done** — placeholder of 5 ₽ per bin + 0 protocol-fee-on-entry breakdown on `PoolAddLiquidityPanel`; tooltip explains it's an estimate until per-bin gas-equivalent lands in Sprint 10 |

### Auto-rebalance + position management

| # | Item | Effort | Status |
|---|---|---|---|
| P1-8 | **One-click "Ребаланс"** on out-of-range positions. Banner exists; button currently navigates to /liquidity. Should: (a) auto-close the OOR position, (b) reopen centred on current active bin with same strategy + similar bin count, (c) idempotent so re-clicking on the new position is a no-op. | M | **done** — banner button now fires `confirmRebalanceAll()` on `PoolDetailPage`. Sequential close-then-reopen per OOR position, preserves capital (currentValueX/Y) + strategy + bin-width-around-active; idempotent per step via random keys; stops on first failure with "Перенесено N из M" |
| P1-9 | **"Закрыть всё"** mass-action on Positions page (already exists on Hedge — copy pattern). | S | **done** — added "Забрать всё" + "Закрыть всё" buttons to `PositionsPage` Card extra; serial-with-stop-on-first-failure shape copied from the HedgePage #6.14 pattern |
| P1-10 | **Position P&L** — current value vs deposit value, both in pair tokens and ₽-equivalent. Meteora's positions table has a P&L column. | M | **done** — new `initial_deposit_x/y BIGINT` cols on `lp_positions` (Liquibase 011 + init-db.sql); `LiquidityService` stamps on first add, scales down proportionally on partial removes; `PositionResponse` surfaces them; new P&L column on `PositionsPage` shows `currentValue - initialDeposit` in pair-quote ₽ + percentage, green/red with arrows. Legacy positions opened pre-migration show "—" with tooltip |

### Backend correctness / observability

| # | Item | Effort | Status |
|---|---|---|---|
| P1-11 | **OHLCV endpoint** in `dlmm-price-oracle`. Aggregate swap events from pool-events Kafka topic into 1m candles, store in ClickHouse, expose `GET /api/v1/oracle/ohlcv/{poolId}?interval=1h&limit=200`. Unblocks P1-4. | M | **done** — `SwapEventOhlcvConsumer` on `pool-events`, `OhlcvAggregator` per-minute buckets with @Scheduled 30s flush, `GET /api/v1/oracle/ohlcv/{poolId}?interval=60&limit=200` clamped at 500. Storage on **Postgres for MVP** (not ClickHouse — ~15k candles/day at seed scale, ClickHouse migration deferred until 1M-row crossover in Sprint 10). New `ohlcv_candles` table with `UNIQUE(pool_id, interval_sec, open_time)` for idempotent re-delivery. 6 unit tests pin the aggregator contract. |
| P1-12 | **Pool TVL rollup is stale.** `pool.totalTvlX/Y` is updated in SwapService and LiquidityService but I never verified the rollup numbers match `sum(pool_bins.reserve_x)`. Add nightly reconciliation job + drift alarm. | M | **done** — new `TvlReconciliationService` cron (`dlmm.pool.tvl-reconciliation-cron`, default 03:34 daily, off-peak vs outbox cleanup at 03:17). Compares `LiquidityPool.totalTvlX/Y` against `SUM(pool_bins.reserve_*)` per pool, records drift into per-pool Micrometer gauges (`dlmm_pool_tvl_drift`), emits WARN at >0.1% relative drift; never auto-corrects. New Grafana panel "TVL rollup drift by pool" |
| P1-13 | **Resilience4j on fee-service and admin-bff.** pool-engine has it; the other two still bare WebClient. | S | **done** — `fee-service.TokenServiceClient` was already wrapped (Sprint 8 C-10); admin-bff: new `BffDownstreamClient` consolidates the five remaining inline calls (user / token / transaction / fee / price-oracle) under per-instance `@CircuitBreaker` + `@Retry`. `AdminService` god-class drops six raw WebClient fields → two wrapped beans. `application.yml` adds matching instance configs. Sequential CB calls in `getPoolAnalytics` / `getTokenAnalytics` replace the Mono.zip that previously blocked on both legs. |
| P1-14 | **Per-tier rate limit metrics** in Grafana. Gateway has `TierBasedRateLimitFilter` (free / pro / enterprise); no panel showing throttle counts per tier. | S | **done** — `TierBasedRateLimitFilter` now increments `dlmm_gateway_ratelimit_total{tier,outcome}` Micrometer counters on every allowed/throttled call. Two new Grafana panels: throttle rate by tier + throttle ratio by tier (saturation indicator). Pre-registered EnumMap so the hot path stays one `counter.increment()` call |

### Monetisation (immediate value)

| # | Item | Effort | Revenue | Status |
|---|---|---|---|---|
| P1-15 | **Enable `protocol_fee_pct = 5%`** on top-3 pools (SUSDT/SRUB, SBTC/SRUB, GAZP/SRUB). Today it's 0%; one config change → captures 5% of every swap fee into Treasury. Estimated 30M ₽/yr at current volume. | S | 30M ₽/yr | **done** — Liquibase `010-enable-protocol-fee-on-top-pools.xml` lowers the column DEFAULT 20→0 (matches 3.A legal memo) and sets `protocol_fee_pct=5` on the three flagship pool ids; `init-db.sql` + `02-extended-assets.sql` mirror the change for fresh deploys; `CreatePoolRequest` validator now enforces `[0,5]` and stops silently bumping `0 → 20` |
| P1-16 | **Sber Treasury as primary LP.** Pitch + integration: Treasury parks corp idle balances in pools, earns fee yield. | L | 100M+ ₽/yr | open — Sprint 10+ |
| P1-17 | **SberSpasibo write-back integration.** Already designed in `docs/SPASIBO-WRITEBACK-DESIGN.md`. Pay-with-spasibo on swap → small fee discount → loyalty hook. | M | 50M ₽/yr | **MVP done** — new `spasibo_writeback_queue` table (Liquibase 009); `SpasiboWritebackService.accrue()` enforces design §3 rate schedule (SWAP 10bps / HEDGE 50bps / ADD_LIQ 5bps / CLAIM_FEE 10bps) + §R-SP-3 anti-gaming thresholds + daily/monthly per-user caps; UNIQUE(dlmm_tx_id) for idempotent re-delivery; @Scheduled flush every 30s. `SpasiboWritebackSwapConsumer` on pool-events. Real HTTP push to Spasibo BU is stubbed (logs payload) until contract+sandbox API land — toggle on by setting `dlmm.spasibo.writeback.endpoint`. 14 unit tests pin cap + threshold + idempotency. |
| P1-18 | **B2B Settlement Rail.** Designed in `SBBOL-INTEGRATION-DESIGN.md`. Corp <→ corp via pool, T+0 settlement, ~50 bps spread. | L | 800M ₽/yr by Q4 | open — Sprint 10+ |

---

## P2 — Polish (Sprint 4-5)

### UX gaps surfaced this session

| # | Item | Effort |
|---|---|---|
| P2-1 | **Quote modal date picker** in admin OTC defaults to "now" — leaves <2-min window. Default to `+24h` so operator just confirms. | S | **done** — default bumped `dayjs().add(30, 'minute')` → `dayjs().add(24, 'hour')` |
| P2-2 | **OTC modal — token + user pickers** instead of raw UUID inputs. Today admin pastes UUID; symbol/email Select would be faster + safer. | S | **done** — UUID Inputs replaced by AntD `<Select showSearch>` for tokenIn/Out (symbol+name) and initiator/counterparty (email+fullName); searchable by typing |
| P2-3 | **Hedge calculator** double-confirm dialog reopens after first Закрыть click — the second confirm is needed because state didn't refresh. Fix: optimistic dismiss on click. | S | **done** — per-hedge `unwindingHedgeIds` Set with optimistic dismiss; per-row spinner/disabled (was global) |
| P2-4 | **`MОЯ ДОЛЯ` formatting** — when share is `<0.01%`, the sub-line "X позиц… · Y ₽" wraps awkwardly in the tile. Shrink font OR split into two lines. | S | **done** — split into two `whiteSpace:nowrap` divs; `KpiTile.sub` widened `string \| ReactNode` |
| P2-5 | **Bin chart Y-axis labels** show "00000.0M" prefix bug — the axis tick formatter drops the leading digit. Fix in `BinLiquidityChart` `tickFormatter`. | S | **done** — extended B/T prefixes, axis widened to 60px |
| P2-6 | **Active bin tooltip** on hover should say "Активный бин · #N · ваша доля X%". Today just shows price + reserves. | S | **done** — tooltip now shows badges (★ Активный бин / Ваш бин), bin id, and "Ваша доля: X%" computed from `userBinSharePctByBinId` map (strategy-weight approximation; exact value from `binAllocations` is Sprint 10) |
| P2-7 | **Notifications dropdown** in user-ui top-right shows badge count (29 right now) but bell click does nothing. Wire up to `dlmm-notification-service` `/me`. | M | **done by inspection** — `NotificationBell` Popover with full list / mark-read / mark-all / margin-alert deep-link is already wired and live in `UserLayout`; backlog note was stale from an earlier sprint where the bell was decorative |
| P2-8 | **Profile page → KYC document upload** — currently text-only fields. Add file picker → Sber ID API. | M | **done (Sber ID stub)** — new `<KycUploadPanel>` on Profile page with three-document Dragger (passport main / passport registration / selfie), per-doc completion chips, client-side type+size validation (JPG/PNG/HEIC/PDF, ≤10 MB), disabled-until-all-uploaded "Отправить на верификацию" CTA. Files stay in the browser; real Sber ID multipart POST swaps in when the contract lands Sprint 10. 4 unit tests pin the NOT_SUBMITTED/REJECTED/PENDING/VERIFIED render branches |

### Admin gaps

| # | Item | Effort |
|---|---|---|
| P2-9 | **PoolDetailPage admin → bin liquidity chart with reserves overlaid** like user side, plus volume sparkline. | M | **done** — `BinLiquidityChart` (admin variant) gained zoom controls + Liquidity ⇄ Reserves (stacked) view toggle (Y blue stack + X-in-Y green stack = canonical depth-chart shape) + the P2-5 Y-axis B/T-prefix fix; new compact volume sparkline above the bin chart on the "Распределение по бинам" tab |
| P2-10 | **TokenDetailPage admin → recent mint/burn events** table. | S | **done** — new "История эмиссии" tab on TokenDetailPage; client-side join over wide-window MINT + BURN fetches with `tokenInId == id \|\| tokenOutId == id` filter; styled rows with green/orange MINT/BURN chip, formatted amount, short user id, status, timestamp |
| P2-11 | **TransactionsPage admin → CSV export with all current filters** (today export-csv ignores filter state). | S | **done** — wave 1 (see above) |
| P2-12 | **SuspiciousTransactionsPage admin → "Mark reviewed"** action that moves flagged tx to an `reviewed_at` column. | S | **done** — `reviewed_at`/`reviewed_by` cols on `transactions` (Liquibase 007 + init-db.sql); new `POST /api/v1/transactions/{id}/review` endpoint (admin-only); admin-bff's suspicious-detection filters out `reviewedAt != null`; per-row "Просмотрено" button invalidates the query so the row vanishes immediately |
| P2-13 | **Audit log** for every admin write action (KYC update, pool pause, OTC accept). Sprint 8 added the `@AdminAudit` aspect in user-service only; needs to extend to transaction-service + pool-engine. | M | **done** — `AdminAuditLog`/`Repository`/`Service`/`Aspect` moved from user-service to `dlmm-common.audit`; new `DlmmAdminAuditAutoConfiguration` activates wherever JPA+AOP+`dlmm.audit.enabled` are present. `admin_audit_log` table mirrored in init-db.sql so it exists at compose-up regardless of which service runs Liquibase first. Annotated: PoolController (CREATE/PAUSE/RESUME/EMERGENCY_SHUTDOWN/FEE_PARAMS_UPDATE/PROTOCOL_FEE_UPDATE), OtcDeskController (CREATE/QUOTE/ACCEPT/REJECT/SETTLE/CANCEL), TransactionController (TX_MARK_REVIEWED) |

### Cross-cutting

| # | Item | Effort |
|---|---|---|
| P2-14 | **i18n EN locale.** Sprint 8 wired up react-i18next on user-ui Swap as a template; ~80% of strings still hardcoded RU. Mechanical sweep. | M | **done** — `dlmm-user-ui/src/i18n/locales/en.json` mirrors `ru.json` 1:1 (≈55 keys: common/brand/auth/dashboard/swap/hedge/pools), with i18next plural forms `_one/_other` for English vs ru's `_one/_few/_many`. Active language persists to `localStorage['dlmm.user.language']` and is re-read on init. New `<LanguageSwitcher>` (RU/EN dropdown) in `UserLayout` header. `i18n.test.ts` (4 cases) is a parity guard — walks both bundles, normalises plural suffixes, asserts conceptual-key parity so a string added to one bundle won't silently miss the other |
| P2-15 | **Dark mode.** Plasma tokens have dark variants; CSS variables already abstracted. Add a toggle in Profile + persist. | M | **done** — three-way Light/Dark/System picker. `themeStore` (`localStorage['dlmm.user.theme']`) applies via `<html data-theme="dark">` on `main.tsx` boot (no FOUC). `sber-theme.css` `[data-theme="dark"]` block overrides every `--bg-*` / `--surface-*` / `--text-*` / `--border-*` / `--shadow-*` token; brand greens stay light values so CTAs read consistently. AntD table-header bg gets explicit overrides (hard-codes its own bg). Cross-tab sync via `storage` event; OS-pref auto-switch when in 'system' mode via `matchMedia`. 6 unit tests pin the contract |
| P2-16 | **Mobile viewport breakpoints.** All cards `xs={24}` but bin chart + OTC tables overflow on phones. | M | **done** — `.sber-pools-search` class drops fixed 320px Input to 100% under 768/414. New `@media (max-width: 414px)` block on both `user-ui/sber-theme.css` and `admin-ui/sber-theme.css` swipes `.ant-modal-body .ant-descriptions-view` / `.ant-table-wrapper` / `.ant-input-group` horizontally (instead of breaking layout) and forces flex-wrap on page-header rows. Verified on 320/375/414 Chrome devtools — OTC desk modal Descriptions, PoolsPage search, PoolDetailPage header all behave |
| P2-17 | **dlmm-ui-common workspace package** to deduplicate `KpiTile`, `TokenPairChip`, `formatCompact`, etc. between admin-ui and user-ui (currently mirrored). | M | **tactical done** — true workspace refactor is L-effort Sprint-10 (Vite alias + tsconfig paths + npm-ci → workspace-install). Tactical fix: `scripts/check-ui-shared-drift.mjs` (manifest-driven byte-level diff watchdog) runs in `frontend.yml` after hex-ratchet and **fails the build** on any drift in `strict: true` shared files. CRLF/LF-normalised. Today's manifest: 4 files (3 strict, 1 documented divergence — KpiTile.sub: ReactNode widening on user side). Promoted admin's `utils/format.ts` to byte-identical with user's so the watchdog is green. Migration plan documented in `docs/UI-SHARED-CODE.md` |

---

## P3 — Parking lot / nice-to-haves

| Item | Why deferred |
|---|---|
| Zap In/Out (deposit any token, auto-swap to pair) | Needs router; >2 weeks |
| Auto-rebalance bots / scheduled rebalance jobs | Defer until manual rebalance is field-validated |
| Farming rewards overlay (extra incentive tokens) | Not until we run a rewards programme |
| Concentrated swap routing across multiple pools (Jupiter-style) | Single-pool routing covers 95% of volume |
| Limit Order partial fills | Limit orders themselves not yet shipped |
| Gas-less swaps via paymaster | Not on roadmap; banking sponsorship covers it |
| WebSocket live price stream on chart | Polling 10s is fine for retail UX |
| NFT receipts for LP positions | Cosmetic; no demand from PO |
| Cross-chain bridge (TON, BNB) | Sber doesn't operate cross-chain |
| Governance token + DAO | Not commercially needed |

---

## Technical debt register

| ID | Area | Note | Effort |
|---|---|---|---|
| TD-1 | Liquibase preConditions hack | Schema changes will MARK_RAN'd because tables already exist from `init-db.sql`. Proper fix: remove `CREATE TABLE` from init-db.sql and move all DDL into Liquibase. | L |
| TD-2 | JWT secret in `docker/.env` is dev-default | Vault / AWS Secrets Manager for prod. | M | **runbook done** — `docs/OPS-SECRETS-RUNBOOK.md` captures production rollout (Sber Vault recommended, Docker Compose secrets fallback, AWS Secrets Manager for cloud-only), bootstrap checklist (≥48-byte secret, per-env separation, audit log SIEM stream), rotation runbook for both `JWT_SECRET` and `DB_PASSWORD`, and explicit "what we're NOT doing" (HSM, per-user keys). Code change to consume Vault is Sprint-10 work blocked on Sber-internal AppRole onboarding |
| TD-3 | Mojibake cyrillic in some pool-engine responses | Double UTF-8 encoding; affects exports. | S | **done** — Dockerfile adds `JAVA_TOOL_OPTIONS=-Dfile.encoding=UTF-8 -Dsun.jnu.encoding=UTF-8` + explicit `-Dfile.encoding=UTF-8` on the entrypoint; pool-engine's `application.yml` adds `server.servlet.encoding.force: true charset: UTF-8` so Tomcat agrees with the JVM |
| TD-4 | No CI/CD pipeline | `.github/workflows/backend.yml` exists from Sprint 2, but no deploy automation. | L | open — Sprint 10 |
| TD-5 | No Helm chart | Docker Compose only. | L | open — Sprint 10 |
| TD-6 | No DB backup story | Postgres data is in a named volume; no scheduled snapshot. | M | **dev-grade done** — `docker/scripts/pg-snapshot.sh` wraps `pg_dump --format=custom -Z 6` with atomic-rename + 14-day retention pruning + cron-ready logging (sample line at 03:17 daily to align with outbox-cleanup off-peak). `docker/backups/` gitignored. `docs/OPS-DB-BACKUP-RUNBOOK.md` covers the dev script, restore drill, AND explicit "what production needs" list: WAL-G + S3 + restore-drill automation. Production WAL-G rollout is Sprint-10 work blocked on Sber Cloud Object Storage bucket setup |
| TD-7 | TokenType extended values not handled in switches | FIAT_BACKED/COMMODITY_BACKED/UTILITY/INDEX_TOKEN behave as opaque labels in business logic. | S | **done** — new `TokenType.requiresUnderlyingAsset()` policy method (off-platform types true: EQUITY/FIAT/COMMODITY/INDEX; synthetic false: LP/GOVERNANCE/UTILITY/STABLE); `TokenService.createToken` validates via the method instead of hard-coding EQUITY_TOKEN; new unit test pins the policy |
| TD-8 | TransactionsPage CSV export ignores filter state | See P2-11. | S | **done** — see P2-11 |
| TD-9 | currentValueX/Y typed as `number?` in user-ui but backend always populates | Tighten to required after backend contract audit. | S | **done** — `Position.currentValueX/Y` tightened from `number?` to `number` in user-ui types; backend `LiquidityService.toResponse` always populates these |
| TD-10 | Per-service JwtAuthenticationFilter duplication (pre-Sprint 9) | Consolidated in `dlmm-common` Sprint 9; audit any remaining per-service copies. | S | **done by inspection** — `find -name JwtAuthenticationFilter.java` returns only `dlmm-common`; all 7 services' SecurityConfigs import `com.sber.dlmm.common.security.JwtAuthenticationFilter`; no per-service copies remain. user-service keeps a *generator* (`JwtTokenProvider`) because it issues tokens, which is the right separation |

---

## Known live issues today

| Severity | Issue | Workaround | Fix |
|---|---|---|---|
| HIGH | Active bin's amountX comes out clamped to 0 (instead of mixed) when composition factor is >1 from prior adds | Composition stays balanced because pure-Y deposit shifts c towards 0 next time | P0-1 |
| MED | Swap with bin step `0.05%` showing `priceImpact > 5%` for amount > pool TVL × 0.5 | Don't swap >TVL/2 (warn in UI) | enhance UI warning |
| MED | Out-of-range warning doesn't appear if all your positions are out-of-range AND there's a 4th KPI tile showing "My share 0%" | UX: tile says 0%, banner doesn't fire | fix detection: compare active bin vs each position range |
| LOW | Notifications counter (29) increments forever; no "mark read" | Hard refresh | P2-7 |
| LOW | Admin Settings → no settings actually mutable for ADMIN role (only SUPER_ADMIN); UI shows greyed-out form with no hint of why | Display readonly fields when role doesn't match | S — copy hint |
| LOW | `dlmm-kafka-init` container exits with code 2 every restart | Init script ran successfully, exits non-0 cosmetically | one-liner: `|| true` |

---

## Sprint rough plan

**Sprint 3 (next 2 wks)** — P0-1, P0-3, P0-4, P0-5, P1-1, P1-2, P1-6, P1-15
   → Fix correctness foundation + Meteora parity wins + 30M ₽/yr revenue switch

**Sprint 4** — P1-3, P1-4, P1-7, P1-8, P1-10, P2-4 through P2-7
   → Limit Orders + chart + rebalance + position P&L + UX nits

**Sprint 5** — P1-11, P1-12, P1-16, P1-17, P2-13 through P2-16
   → OHLCV + Treasury LP + Spasibo + EN locale + dark mode

**Sprint 6+** — P1-18 (B2B rail), polish, scale

---

## Open questions for PO

1. Limit Orders P&L tracking — separate column or merged into Positions?
2. P0-1 L-units refactor — can we accept a 1-day prod freeze for migration, or do we need zero-downtime?
3. Auto-rebalance — opt-in toggle per position, or platform default?
4. EN locale — full coverage or just key surfaces (Swap, OTC desk for foreign counterparties)?
5. Dark mode — Sber brand says always-light for banking; do we override on user-ui only?
