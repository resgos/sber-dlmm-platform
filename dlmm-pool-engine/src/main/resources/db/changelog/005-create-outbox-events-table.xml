<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.24.xsd">

    <!--
      Pool-engine's view of the shared outbox_events table. Identical schema
      to token-service's changeset (which creates it first); this one is
      MARK_RAN on existing DBs where the table already exists.
    -->
    <changeSet id="005-create-outbox-events-table" author="dlmm-platform">
        <preConditions onFail="MARK_RAN">
            <not><tableExists tableName="outbox_events"/></not>
        </preConditions>
        <createTable tableName="outbox_events">
            <column name="id" type="UUID" defaultValueComputed="gen_random_uuid()">
                <constraints nullable="false" primaryKey="true" primaryKeyName="pk_outbox_events"/>
            </column>
            <column name="aggregate_type" type="VARCHAR(64)">
                <constraints nullable="false"/>
            </column>
            <column name="aggregate_id" type="VARCHAR(128)">
                <constraints nullable="false"/>
            </column>
            <column name="event_type" type="VARCHAR(128)">
                <constraints nullable="false"/>
            </column>
            <column name="topic" type="VARCHAR(128)">
                <constraints nullable="false"/>
            </column>
            <column name="payload" type="TEXT">
                <constraints nullable="false"/>
            </column>
            <column name="created_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
            <column name="published_at" type="TIMESTAMP"/>
            <column name="attempts" type="INT" defaultValueNumeric="0">
                <constraints nullable="false"/>
            </column>
            <column name="last_error" type="TEXT"/>
            <column name="service" type="VARCHAR(32)" defaultValue="pool-engine">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <sql>
            CREATE INDEX IF NOT EXISTS idx_outbox_unpublished_per_service
              ON outbox_events (service, created_at)
              WHERE published_at IS NULL;
        </sql>
    </changeSet>

</databaseChangeLog>
